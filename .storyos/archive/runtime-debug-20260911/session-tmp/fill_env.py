# -*- coding: utf-8 -*-
import json, io, base64
# We build the delta in Python directly as a dict literal, but to avoid quoting hell
# we read it from a separate JSON file that we write from JS base64 below.
print("READY")

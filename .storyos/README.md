# .storyos

Story OS 内部维护目录，不是人工生产配置入口。

- `history/install/`：历史安装与补丁记录。
- `history/migrations/`：迁移账本。
- `history/legacy/`：已被新结构替代但暂时保留的兼容文件。

人工配置统一位于 `config/storyos.yaml`，仓库索引位于 `config/index.yaml`。


# History

这些文件仅用于安装、补丁和迁移追溯，正常 Story/Storyboard/Image/Release 流程禁止默认读取。

- `install/`：历史安装标记，文件内容保持原样。
- `migrations/`：迁移账本。
- `legacy/`：已被 `config/index.yaml` 替代的旧索引等兼容资料。

只有排查升级失败、回滚或版本来源时才进入本目录。

# Dali Cat Story OS — Repository Execution Contract

> 这是 `aigc-dali-cat/story` 的 Agent 执行入口，不是第二套创作规范。
> **发生冲突时，以 `standards/制作规范_正式版.md` 为唯一权威。**

## 1. 任务开始前必须读取

按以下顺序：

1. `README.md`：确认当前剧集索引与仓库结构。
2. `standards/制作规范_正式版.md`：唯一主规范。
3. 与任务有关的从属执行细则：
   - 推流/发布：`standards/抖音推流评分与发布后漏斗规范_V1.4.md`
   - 真实性：`standards/真实性与共享风格锚点规范_V1.1.md`
   - 字幕语言：`standards/字幕人话化与声音卡规范_V1.1.md`
   - 字幕视觉：`standards/最终字幕视觉规范_V1.0.md`
   - 若单集明确采用共享风格锚点，再读取对应风格文件。
4. 目标剧集的 `README`、`docs/`、已锁定分镜/交接文件。
5. 若目标单集存在 `episode.yaml`，必须读取；其用途是描述**本集已锁定事实**，不能覆盖主规范。

## 2. Agent 状态机

只有满足上一阶段门禁，才进入下一阶段：

`选题候选 → 同质化门禁 → 故事闭环 → 四张视觉准入 → 正式分镜 → 连续性锚点 → 生图 → 字幕 → 制作验收 → 发布门禁 → publish → 数据复盘`

禁止因为用户说“直接出图”就跳过已经明确要求的剧情锁定/真实性/连续性检查；若前置资料已在当前单集落盘，则直接复用，不重复询问。

## 3. 四类硬门禁

### A. STORY_GATE
- 最近5篇账号级同质化已检查。
- 四把锁差异数满足主规范。
- 任务/事件闭环完成。
- 高潮和终局帧明确。
- 结尾不是用“未完待续”代替本篇结局。

### B. VISUAL_GATE
- 正式图文比例为 9:16，目标 1080×1920（除明确标注为非发布辅助图的资产）。
- 四张视觉准入已完成并通过后，才批量生产。
- 人物、服装、房屋/空间、关键道具、时间/天气等连续性锚点不得无因漂移。
- 不以统一压暗、统一噪点、商业棚拍/概念海报感代替真实现场。

### C. SUBTITLE_GATE
- 正式字幕前必须完成声音卡。
- 每帧必须在“有字幕”或“明确静默图”二者之一。
- 连续三图朗读不能像 20 张独立悬疑海报。
- 被强调线索必须有后果/回收，或降级为普通细节。

### D. RELEASE_GATE
- publish 目录帧数与 `episode.yaml` 一致。
- 数字编号连续，无缺图、重复图号。
- release 模式下所有要求的 review 项必须 `passed`。
- 所有被标记为锁定的底图/资产 SHA-256 不得变化。
- 标记为 `subtitle_only` 的修改不得通过重新生成底图完成。

## 4. 修改范围协议

用户的修改指令优先解释为“最小改动”。

- “只改字幕 / 底图别动 / 一像素尽量别动” → `subtitle_only`。
- “保持原画风重做” → 允许重生成目标帧，但必须继承该集 continuity/style anchors。
- “锁定” → 将已确认事实写入 `episode.yaml` 的 `locks` 或单集 docs，后续不得擅自漂移。
- 未被点名的已通过帧，不因修一张图而批量重做。

## 5. 机器校验

新篇建议把 `standards/templates/episode.template.yaml` 复制到单集根目录为 `episode.yaml`。

开发中：

```bash
python skills/dali-cat-story/scripts/validate_all.py path/to/episode.yaml
```

发布前：

```bash
python skills/dali-cat-story/scripts/validate_all.py path/to/episode.yaml --release
```

机器校验通过不代表视觉语义必然通过；涉及“是否真实、是否有滑下去欲望、高潮是否够强、人物是否像同一个人”等，仍必须做人工/多模态审核。

## 6. 交付约定

每篇正式交付至少能追溯到：

- 单集 README / 当前状态
- 最终剧情或分镜
- `episode.yaml`
- 字幕源文件（若有字幕）
- publish 资产目录
- 审核结果
- 发布标题 / 简介 / 话题（发布阶段）

禁止只交一组图片而没有可追溯的剧情、字幕和审核状态。

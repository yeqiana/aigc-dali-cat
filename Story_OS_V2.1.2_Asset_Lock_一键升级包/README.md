# Story OS V2.1.2 Asset Lock 一键升级包

目标：
新增 Asset Lock 层：
Story Lock
 -> Asset Lock
    -> Character Master
    -> Location Master
    -> Device Master
 -> Visual Lock
 -> Production

本升级包用于增加资产母版管理能力，不修改已有故事资产。
安装前会创建备份。

安装：
python -X utf8 install.py --repo <story仓库路径>

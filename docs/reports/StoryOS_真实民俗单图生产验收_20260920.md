# StoryOS 真实民俗单图生产验收（2026-09-20）

## 1. 验收范围

目标 Episode：`episodes/尸解仙`

本轮固化真实生产 Frame05 的端到端证据，并验证 Production Ledger 在 `storage.episode_meta_store.mode=mysql` 下的权威性与恢复能力。全过程未提交代码、未重新生成 Frame05、未生成 Frame02。

当前生产配置：
- Episode Meta Store：MySQL
- Runtime Store：MySQL
- Hot State：Redis
- Production Ledger：single-writer
- Image Runtime：CODEX image lane
- Normalize：NP01

## 2. Frame Contract

Frame05 当前 Queue / Ledger 绑定：
- Frame Contract：`meta/runtime/contracts/frames/05.json`
- `frame_contract_sha256`：`394848b7d949cd554e08b9b8bae0a4e58d69f734dffc11fc5a34b53fba1718bd`
- Queue Item：`b1bfe40f4819`
- Batch：`BATCH_1b3c157ea03e`
- Runtime Transaction：`c29e89af630e4249accee5de79dd8160`

Frame Contract SHA 同时出现在 Queue、Worker Lifecycle、Prompt Package 和 MySQL Ledger attempt 中，生产绑定一致。

## 3. Prompt Compile

正式生产 Prompt：
- 文件：`episodes/尸解仙/prompts/production/05.txt`
- 文件 SHA-256：`3ad76df6245254985200de3bb237449f199c26e6935ca8340fa39ac4008665b6`
- Prompt Package SHA-256：`f7d2a26ba3cc3667553111e411464a7c374fe8586fa976cd059068c1152f0152`
- Scene Prompt SHA-256：`1a9693f9c2e42c39313bdf1e8bb0ccbd6b8cdd1c82c42dd86e007961d9197421`
- Ledger request `prompt_sha256`：`1a9693f9c2e42c39313bdf1e8bb0ccbd6b8cdd1c82c42dd86e007961d9197421`
- Model：`gpt-image-2`
- Quality：`high`

说明：文件字节 SHA 与 Scene Prompt SHA 的计算对象不同；真正用于 generation request 绑定的是 Scene Prompt SHA，Queue / Lifecycle / Ledger 三处一致。

## 4. Queue

通过 `scheduler_core.load_queue()` 读取 canonical hot-state，而不是直接读取旧 `meta/production-queue.json`。

Frame05 canonical Queue：
- status：`generated`
- attempts：1
- queued_at：`2026-09-20T22:53:14+08:00`
- started_at：`2026-09-20T22:54:30+08:00`
- completed_at：`2026-09-20T23:02:20+08:00`
- transaction_id：`c29e89af630e4249accee5de79dd8160`
- execution phase：`COMMITTED`
- output：`episodes/尸解仙/media/candidates/scheduled/05-b1bfe40f4819-a1.png`

直接读取旧 Queue JSON 时 Frame05 为 0 行，但 canonical Queue 能正确返回该任务，证明 Redis hot-state 才是当前 Queue 读路径，旧 JSON 不可作为判断依据。

## 5. Worker

Worker Lifecycle：
- 文件：`meta/image-workers/05-b1bfe40f4819-a1.lifecycle.json`
- state：`SUCCEEDED`
- worker_pid：19492
- attempt：1
- backend：`codex_subscription`
- elapsed_seconds：98.62
- raw：`media/raw/05-1789916127.png`
- candidate：`media/candidates/scheduled/05-b1bfe40f4819-a1.png`

Worker 原始日志：
- `meta/image-workers/05-b1bfe40f4819-a1.jsonl`
- 日志明确记录 `Image generated successfully.`
- Codex analytics 上报失败与若干非图片插件未安装告警均未影响图片生成结果。

## 6. Provider Receipt

Ledger / Lifecycle 绑定的 Provider Receipt：
- path：`episodes/尸解仙/meta/provider-receipts/05-1789916226.json`
- SHA-256：`72162d99a8f9a399b4e37a462b221226811d2b2de254e5732421c5f38331764a`
- capability：`OPENAI_GPT_IMAGE_2_DESKTOP_V1`
- provider：OpenAI
- generation route：`codex_subscription`
- provider raw canvas：1122 × 1402
- requested/release canvas：1080 × 1350
- ratio delta：0.000356633
- normalize decision：`AUTO_NORMALIZE`
- Ledger receipt `storage_source`：`mysql`

Receipt 明确声明 `receipt_is_evidence_not_authority=true`，符合“Provider Receipt 是执行证据，不是业务 authority”的设计。

## 7. Normalize

NP01 Normalize 结果：
- source size：1122 × 1402
- target size：1080 × 1350
- operation：`RESIZE_LANCZOS`
- ratio_delta：0.000357
- crop_applied：false
- reencoded：true
- local_attempts：1

没有使用裁切掩盖比例问题；比例偏差落在自动 Normalize 阈值内。

## 8. Candidate

最终 candidate：
- `episodes/尸解仙/media/candidates/scheduled/05-b1bfe40f4819-a1.png`
- size：2,457,695 bytes
- dimensions：1080 × 1350
- SHA-256：`27c0bbb197b0bcdb4cb6204e8c0e36eba65f3e7cf8f3bf1b25489089bab93cfd`

Worker Lifecycle、MySQL Ledger current_candidate 与本地文件 SHA 一致。

## 9. Ledger

通过 `production_ledger.load_authority()` 读取 MySQL authority，Frame05：
- status：`ORIGINAL_READY`
- attempt_id：`e9b09232e087`
- attempt result：`success`
- candidate SHA：`27c0bbb197b0bcdb4cb6204e8c0e36eba65f3e7cf8f3bf1b25489089bab93cfd`
- provider receipt SHA：`72162d99a8f9a399b4e37a462b221226811d2b2de254e5732421c5f38331764a`
- content_repairs_used：0
- technical_failures：[]

当前代码中的 `get_ledger()` 已改为统一走 `load_authority()`；MySQL 模式不再把旧 `production-ledger.json` 当读 authority。

## 10. Recovery

Frame05 首次 candidate commit 暴露了真实恢复场景：

Production Commit Journal：
- phase：`COMMITTED`
- reason：`CANDIDATE_COMMIT_FAILED: frame has no generation attempt`
- recovery：`ledger_ready_replayed`

Reconciliation：
- reconciled_at：`2026-09-20T23:02:20+08:00`
- outcome：`LEDGER_READY_REPLAYED`
- ledger_status：`ORIGINAL_READY`
- lifecycle_state：`SUCCEEDED`

这说明 Worker 已成功、Ledger 也已具备 ready 状态，但 scheduler 在一次 commit 读取中遇到“看不到 generation attempt”的旧 authority 读取问题；reconcile 通过 MySQL Ledger + Worker Lifecycle 重新对齐后，没有二次调用图片 Provider，最终恢复成功。

## 11. MySQL Authority Crash-Recover 实库验收

使用隔离验收 ID `EPU_ACCEPT_20260920_CRASHRECOVER`，对本机真实 MySQL `story_os_runtime` 执行：

`begin/pending → MySQL persist → 进程退出边界 → 新 Python 进程 recover → success persist → 再读验证`

结果：
- MySQL health：alive，MySQL 8.0.46
- begin persist：`mysql_written=true`
- begin authority SHA：`45971df79420f1726d38095b792b68022a4b1b5f9522c02a325eda455f0376b7`
- 新进程 recover：`GENERATING / pending`
- success persist authority SHA：`a2456cba6befd8b4685093504e465969490dd1603ccbe477e67a83c68d8149f8`
- final：`ORIGINAL_READY / success`
- cleanup：`CLEANUP_REMAINING=0`

另外，系统测试明确覆盖：MySQL 模式即使存在 stale `meta/production-ledger.json`，也从数据库 authority 读取；`get_ledger()` 同样不得回退旧文件。

## 12. Final SHA

本次真实民俗单图最终验收 SHA：

`27c0bbb197b0bcdb4cb6204e8c0e36eba65f3e7cf8f3bf1b25489089bab93cfd`

对应：
`episodes/尸解仙/media/candidates/scheduled/05-b1bfe40f4819-a1.png`

## 13. 暴露问题

1. Frame05 真实生产过程中曾出现 `CANDIDATE_COMMIT_FAILED: frame has no generation attempt`。
   - 根因方向：生产 Ledger 读路径曾混入 compatibility JSON / 非当前 authority 视图。
   - 结果：Worker 已成功、MySQL Ledger 已 ready，但首轮 commit 未正确看到 active attempt。
   - Recovery 已通过 `LEDGER_READY_REPLAYED` 自动收敛，没有重复生图。

2. 旧 Queue JSON 不能代表当前 Queue。
   - 直接读旧 `meta/production-queue.json` 不能代表当前热状态。
   - `scheduler_core.load_queue()` 当前从 Redis hot-state 读取 authority。

3. 《尸解仙》Frame02 已解除此前 Pixel Master 物理资产缺失阻塞。
   - Pixel Master：`episodes/尸解仙/media/candidates/scheduled/01-a171db41184d-a2.png`
   - 实际 SHA-256：`d6aaf0f185b21895128fe8b911902a12096487d19ede01a5244aa7dc3dba71c7`
   - 与 `character-pixel-master.json` 冻结 SHA 一致。
   - P01 crop SHA：`3c6fb2aec0da6a4032eed3e7f862d4a61538aa5d592c4d47c287b4610dea87c8`
   - P02 crop SHA：`76117ea5e0e5aa28197a3584175ab671638eb5f8712a4fffc1de9753ac0d7d4f`
   - 两个 crop 均与 `character-master-crops.json` 清单一致。

4. Frame02 已完成一次真实 Provider 出图并提交 MySQL Ledger。
   - 初始审计时 Redis Queue 为 `status=queued / attempts=0`；期间暴露并修复了 `QUEUE` 1800 秒 TTL 会静默丢失待生产 item 的问题，恢复后 Redis `TTL=-1`。
   - 使用现有 `image_scheduler.py run --max-workers 1` 消费原 Queue item `c1554c7d2741`；未创建新的 scheduler 实现或后台调度器。
   - Worker lifecycle：`SUCCEEDED`；Recovery transaction=`506e5a730be7429c8ba61d30bd2a548f`。
   - Provider：OpenAI `gpt-image-2`，generation route=`codex_subscription`。
   - P02/P01 两张 identity crop 均通过 `codex_subscription_cli_attachment` 传入 Provider，Ledger `reference_execution.passed_to_provider=true`、`verified=true`。
   - RAW：`media/raw/02-1789959498.png`，SHA=`ae3c99093e12f6b9641efbfad87d320b758cc391ace22f3009436353684f8951`，原始画布 1122×1402。
   - Normalize：`RESIZE_LANCZOS` → 1080×1350，无裁切。
   - Candidate：`media/candidates/scheduled/02-c1554c7d2741-a1.png`，SHA=`48c558b1dc8125dc8b49bde9f1b2c7b930bfe9abcf6579f8771f3ca97cb5f9d7`。
   - Provider Receipt 已持久化到 MySQL，receipt id=`PR_9838f9fa3434ba1f80fa8ad2100f97ecdd472289`；旧 JSON 路径只作为 legacy locator。
   - 最终 Queue=`generated / attempts=1 / execution.phase=COMMITTED`；MySQL Ledger=`ORIGINAL_READY`，attempt result=`success`。
   - 再执行一次 reconcile 得到 `LEDGER_READY_REPLAYED`，且 Queue attempts 与 Ledger attempts 均仍为 1，证明恢复链不会重复调用 Provider。

## 14. 修复记录

当前未提交工作区中已经存在并经本轮验证的 Ledger / Recovery authority 修复：
- `episodes/_system/production_ledger_core.py`：`get_ledger()` 统一改为 `load_authority()`。
- `episodes/_system/production_recovery.py`：reconcile 每轮通过 `production_ledger.load_authority()` 重新加载 Ledger authority。
- `episodes/_system/production_recovery_persistence.py`：MySQL / dual 模式将 recovery transaction 写入 MySQL repository。
- `platform/repository/mysql/mysql_production_recovery_journal_repository.py`：`TB_PRODUCTION_RECOVERY_JOURNAL` 保存 mutable recovery transaction document，并以 SHA256 + BYTE_SIZE 校验完整性。
- MySQL 模式下 `production_recovery._journal()` 在 MySQL persist 后直接返回，不再写 `production-commit-journal.json`；旧 JSON 仅保留 json/dual 兼容路径。

本轮验收同步：
- Pixel Master 已恢复并通过冻结 SHA 校验。
- P01/P02 crop 已恢复并通过清单 SHA 校验。
- Frame02 当前 `contract_sha256=13a41c746c21fa45e471b94bf70a515e3b8cbecb6a2d800ed0cc0a91269db5a7`。
- `frame_contract.verify_frame(..., 2)` 返回空错误集，Frame Contract verify PASS。
- `character_appearance_anchor.verify()` 返回空错误集，Appearance Anchor PASS。
- Frame02 identity chain：P02、P01 均为 `anchor_state=bound`。
- Reference Arbitration（batch）PASS：选中 P02 crop + P01 crop，两条 identity reference SHA 与冻结 crop SHA 完全一致。
- Prompt Package 已存在于 MySQL persistence；`frame_contract_sha256` 与 Frame02 合同一致，`package_sha256=ff0d868f8736d7679bd62ac75bdcf568a81697bad87614bd7f908ab7f237e6e8`。
- Redis Queue 初次审计存在 Frame02 item `c1554c7d2741`，`status=queued`、`attempts=0`；Queue 内 Frame Contract / Prompt Package / 两条 identity reference SHA 均与当前链路一致。
- 收尾期间发现 Queue 的 1800 秒 TTL 会静默删除仍待生产的 item，这是本轮新暴露的真实生产问题。
- `platform/state/storyos_hot_state.py`：将 `HotStateSpec.ttl_seconds` 扩展为可选值，并将 `QUEUE` TTL 从 `1800` 改为 `None`；Redis `SET ex=None` 后待生产 Queue 不再因空闲窗口过期。
- `tests/system/test_production_queue_hot_state_bridge.py`：新增 `test_production_queue_hot_state_does_not_expire_when_idle`，锁定 Queue `expire_seconds=None`。
- Queue 恢复前重新校验 Frame02 Contract SHA、scene prompt SHA、P01/P02 crop SHA；全部一致后通过 `scheduler_core.save_queue()` 恢复原 item。
- 使用现有 image scheduler 单 worker 执行 Frame02；真实 Provider 调用成功，RAW / Provider Receipt / normalized Candidate 均落地。
- Candidate SHA=`48c558b1dc8125dc8b49bde9f1b2c7b930bfe9abcf6579f8771f3ca97cb5f9d7`；MySQL Ledger=`ORIGINAL_READY`；Queue=`generated`；Recovery=`COMMITTED`。
- Provider Receipt 由 `provider_receipt_persistence.load_by_path()` 从 MySQL 读取，`source=mysql`，两张 reference SHA 与入队冻结 SHA 完全一致。
- reconcile 复跑结果=`LEDGER_READY_REPLAYED`，attempt 数保持 1，未产生重复候选。
- `auto_review_loop` 规则审计：Frame02 identity continuity findings=0、Visual Profile PASS；当前唯一规则缺口是 `story_semantic_trace_missing`。
- Final Semantic Critic 是整集 frame-set gate；当前 Episode 尚有多帧 PENDING，因此不能为了单图验收伪造 Frame02 的最终语义 PASS。

## 15. 测试结果

Production Ledger / Recovery 定向回归：

`python -m pytest tests/system/test_production_ledger_persistence.py tests/system/test_production_ledger_authority_facade.py tests/system/test_runtime_recovery.py -q`

结果：

`46 passed in 6.37s`

Identity / Reference Arbitration / Recovery Journal MySQL migration 定向回归：

`python -m pytest tests/system/test_reference_arbitrator_identity_requirements.py tests/system/test_identity_continuity_evidence.py tests/platform/test_mysql_production_recovery_journal_repository.py tests/system/test_production_recovery_persistence.py -q`

结果：

`33 passed in 2.17s`

Production Queue TTL / storage boundary 回归：

`python -m pytest tests/system/test_production_queue_hot_state_bridge.py tests/system/test_production_queue_storage_boundary.py -q`

结果：

`10 passed in 0.77s`

真实 Frame02 出图后合并定向复跑：

`python -m pytest tests/system/test_production_ledger_persistence.py tests/system/test_production_ledger_authority_facade.py tests/system/test_runtime_recovery.py tests/system/test_production_queue_hot_state_bridge.py tests/system/test_production_queue_storage_boundary.py tests/system/test_reference_arbitrator_identity_requirements.py tests/system/test_identity_continuity_evidence.py tests/platform/test_mysql_production_recovery_journal_repository.py tests/system/test_production_recovery_persistence.py -q`

结果：

`89 passed in 13.58s`

实库验证（既有本轮证据）：
- MySQL health PASS
- begin persist PASS
- fresh-process recover PASS
- success persist PASS
- cleanup PASS

## 16. 验收结论

Frame05 真实单图链路已具备可审计闭环：

`Frame Contract → Prompt Compile → Redis Queue → Worker → Provider Receipt → Normalize → Candidate → MySQL Ledger → Recovery → Final SHA`

Production Ledger authority 已确认是 MySQL；Production Recovery Journal authority 也已迁到 MySQL。MySQL 模式下旧 `production-ledger.json` / `production-commit-journal.json` 均不应参与权威读取或写入。

Frame02 已完成真实生产闭环：

`Frame Contract → Character Visual Contract → Appearance Anchor → Pixel Master → P01/P02 Crop → Reference Arbitration → Prompt Package → Redis Queue → Worker → Provider → RAW → Normalize → Candidate → MySQL Provider Receipt → MySQL Ledger → Recovery`

最终 Candidate SHA=`48c558b1dc8125dc8b49bde9f1b2c7b930bfe9abcf6579f8771f3ca97cb5f9d7`，Ledger=`ORIGINAL_READY`，Recovery=`COMMITTED`；reconcile 重放保持单 attempt，不重复生成。

单图规则审计中 identity continuity 与 visual profile 均通过。当前尚不能宣称 Frame02 最终 Semantic PASS，因为 Final Semantic Critic 是整集 frame-set gate，而《尸解仙》仍有未生产帧；这属于正确的全局审核边界，不是本次真实出图链路失败。

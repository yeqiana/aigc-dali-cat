# Story OS V2 残留清理分类清单

日期：2026-09-11

目的：

在不影响 EP003 当前生产、不破坏 V3 Runtime 能力的前提下，对 V2 演进阶段文件进行分类管理。

原则：

- 不直接删除。
- 先分类。
- 先归档，再删除。
- 代码必须经过引用扫描。

---

# 一、可归档（低风险）

## .storyos 历史运行备份

路径：

```
.storyos/backups/
.storyos/smoke/
```

示例：

```
v2511_forced_candidate_gate
v260_performance_runtime
v240_batch_5img
```

处理：

```
.storyos/archive/v2-runtime/
```

原因：

- 历史验证产物。
- 不参与当前生产。
- 保留迁移证据。

状态：✅ 已归档。

归档位置：

```
.storyos/archive/v2-runtime/backups/
.storyos/archive/v2-runtime/smoke/
```

归档日期：2026-09-11。迁移前后逐文件 SHA-256 一致（backups 73 个文件、smoke 1 个文件）。

---

# 二、保留（历史资产）

## V2 架构文档

路径：

```
docs/Story_OS_V2.*
```

处理：

保留，后续整理：

```
docs/history/v2/
```

原因：

- 记录 V2 → V3 演进。
- 产品化需要架构演进证据。
- 不属于垃圾文件。

状态：保留。

---

# 三、禁止删除（可能被 V3 使用）

## Runtime 能力代码

路径：

```
episodes/_system/*v21*
episodes/_system/*v22*
episodes/_system/*v26*
```

重点：

```
visual_lock_v21.py
visual_narrative_core_v22.py
capture_grammar_v226.py
reference_arbitrator.py
```

原因：

这些虽然带 V2 名称，但已经成为 V3 Runtime 能力基础。

状态：禁止清理。

---

# 四、优先整理（生产临时文件）

发现：

```
.storyos/_ep003_prod_driver.py
.storyos/dump_task.ps1
.storyos/task_dump.txt
reports/debug.log
```

处理：

迁移：

```
.storyos/runtime-debug/archive/
```

原因：

- 调试辅助文件。
- 不属于正式生产资产。
- 避免进入产品仓库。

状态：已完成，见下方「Runtime Debug 临时产物归档」。

## Runtime Debug 临时产物归档

状态：✅ 已归档

位置：.storyos/archive/runtime-debug-20260911/

说明：

- 不属于生产资产
- 不属于 Runtime 核心代码
- 保留调试证据
- 不影响 EP003

迁移方式：同盘 git mv（内容不变、SHA-256 不变、Git 历史可追踪）。归档文件数 7，迁移前后逐文件 SHA-256 一致（hashDiff=0）。.storyos/_ep003_prod_driver.py 在执行前已被其他进程移除，未纳入本次归档。

### 第二批：根目录临时产物（2026-09-11 追加）

状态：✅ 已归档

位置：.storyos/archive/runtime-debug-20260911/

已归档文件（6）：

```
_tmp_contact_sheet.py
_tmp_critic_decode.txt
_tmp_review_out.txt
_tmp_validate_out.txt
_tmp_codex_shim_0153.py
runtime-smoke-report.json
```

说明：

- 均为仓库根目录遗留的临时调试/历史产物，不属于生产资产与 Runtime 核心代码。
- _tmp_codex_shim_0153.py 为已失效的历史 Codex shim；迁移后 contract_sync.py 的根目录守卫按“不存在即通过”处理，校验仍 PASS。
- runtime-smoke-report.json 为 2026-09-10 快照；runtime smoke 脚本仍会在仓库根目录重新生成同名默认输出，因此该文件会再次出现，按“本地可再生”处理。

迁移方式：git mv（内容不变、SHA-256 不变、Git 历史可追踪）。第二批 6 个文件迁移前后逐文件 SHA-256 一致（hashDiff=0）。

暂未迁移（按“运行中依赖不修改”处理，待确认后再归档）：

- .storyos/fc.log、.storyos/na.log、.storyos/rebind.log、.storyos/transition.log（2026-09-11 18:14–18:15 由 EP003 当前运行写出，已被并发线程暂存）
- reports/debug.log（Chromium/crashpad 杂项日志，2026-09-11 15:26，已被并发线程暂存）
- workbench/_f01_run.log、workbench/_ve.log（workbench 活动工作目录内日志）

---

# 四之二、EP003 废弃归档（2026-09-11）

## EP003 abandoned archive

状态：✅ archived

归档前路径：

```
episodes/10_彼此的天上/03_雾中的另一座生活区/
```

归档位置：

```
episodes/_archive/20260911_EP003_abandoned_雾中的另一座生活区/
```

处理方式：同盘 `git mv` 目录级重命名，不涉及内容改写。文件内容、JSON/YAML、时间戳逻辑与任何已生成资产均未改动，也未重新生成资产。

迁移前清单：`episodes/_archive/EP003_abandoned_manifest_20260911.txt`（445 个文件、206785859 字节），迁移前后逐文件 SHA-256 100% 一致（missing=0 / extra=0 / diff=0）。

保留证据：`meta/`、`production/`、`prompts/`、`release/`、`docs/`、`media/` 全量保留，无删除。

机器状态：归档内 `meta/episode-state.json` 保持 `PRODUCTION_PASSED` 原样，未回退、未越级；废弃决定记录在 `meta/episode-abandoned.json`（该标记不是第二状态机）。

扫描豁免：`episodes/_archive/.storyos-non-episode.json`（沿用仓库既有 non-Episode 约定；该标记被 `.gitignore` 排除，属本地文件）。

结论：不影响 EP001（`01_不存在的夜行路`）、EP002（`02_玻璃另一边的手`）、Runtime 核心代码与图片生产链。

发现机制闭环（2026-09-11 已执行）：`episodes/_system/episode_discovery.py` 的 `EXCLUDED_PARTS` 已加入 `_archive`（原为 `_system/_tests/__pycache__`），并补充 `self_test` 断言。此后即使新克隆仓库缺少本地豁免标记 `episodes/_archive/.storyos-non-episode.json`，归档目录也不会再被识别为活跃 Episode。`python episodes/_system/episode_discovery.py` 自检 PASS。

---

# 四之三、本轮风险排序收尾（2026-09-11）

按风险从低到高执行，全部为历史资产整理，不涉及生产代码重构。

## 1. `_archive` 发现机制

状态：✅ 已修复

文件：`episodes/_system/episode_discovery.py`

- `EXCLUDED_PARTS` 增加 `_archive`。
- `self_test` 增加 `_archive` 断言。
- 自检：`python episodes/_system/episode_discovery.py` → PASS。

## 2. EP003 悬空 inventory / checksum

状态：✅ 已修复

文件：

```
episodes/10_彼此的天上/checksums.sha256
episodes/10_彼此的天上/PACKAGE_INVENTORY.md
episodes/10_彼此的天上/README.md
```

- `checksums.sha256`：移除指向已归档 `03_雾中的另一座生活区` 的 6 行，并加注归档指针。
- `PACKAGE_INVENTORY.md`：6 行清单替换为 1 行归档说明。
- `README.md`：废弃说明中的路径改为 `../_archive/20260911_EP003_abandoned_雾中的另一座生活区/...`。
- 未改动生产资产与归档目录内证据；`meta/episode-state.json` 仍为 `PRODUCTION_PASSED`。

## 3. `.storyos_tmp` 归档

状态：✅ 已归档

- 原路径：`.storyos_tmp/`
- 新路径：`.storyos/archive/runtime-debug-20260911/session-tmp/`
- 55 个文件、11.66 MB，`git mv` 100% rename，内容与 SHA-256 不变。
- 引用扫描：全仓仅 3 处（`workbench/_rel_critic.py`、`_rel_critic2.py`、`_rel_stage.py`），且其 root 指向另一仓库，与本仓库无关 → 不修改。

## 4. 根目录 V2 手册

状态：✅ 已归档

- 原路径：`Story_OS_V2.1_运行模式与使用示例手册.md`
- 新路径：`docs/archive/v2-evolution/Story_OS_V2.1_运行模式与使用示例手册.md`
- `git mv`，内容不变。
- 引用扫描：全仓仅 `reports/全仓优化分析_20260903_V1.0.md` 提及（历史报告，不修改）。

结论：四项均为低风险历史资产整理，不影响 EP003 生产、Runtime 核心、图片生产链。

---

# 五、清理流程

```
扫描引用
    ↓
分类
    ↓
归档
    ↓
运行测试
    ↓
观察EP003
    ↓
最终删除
```

---

# 六、当前禁止操作

禁止：

- 删除所有 V2 文件。
- 批量删除 episodes/_system。
- 删除 visual/capture/runtime 相关文件。
- 在 EP003 生产期间进行大规模迁移。

---

最终目标：

```
V2 历史能力
      ↓
archive/history

V3 当前生产能力
      ↓
保持干净

临时调试文件
      ↓
隔离删除
```

# Story OS 文档生命周期清理清单

日期：2026-09-11

目的：

整理 V2 → V3 演进过程中产生的历史文档、规范和模式资产。

原则：

- 不删除有历史价值的设计资产。
- 不影响 EP003 当前生产。
- 不移动当前 V3 核心规范。
- 历史经验转化为 Pattern Library。

---

# 一、归档（Archive）

## V2 演进文档

路径：

```
docs/Story_OS_V2.*
```

处理：

```
docs/archive/v2-evolution/
```

原因：

- 已被 V3 架构吸收。
- 保留迁移历史和设计演进证据。

---

# 二、模式库（Pattern Library）

## 视觉与提示词经验

包括：

- 现实侵入式伪纪录片模板
- 导演镜头光影规则
- 环境物理与异常隐藏规则
- 真实性与共享风格锚点
- 设备物理档案

建议：

```
library/visual-patterns/
```

定位：

不是生产规范，而是 Agent 生成能力素材。

---

# 三、当前保留（Active）

以下保持：

```
standards/templates/
standards/Runtime_*
standards/Story_Agent_Runtime_V1.0.md
standards/Resolved_Frame_Contract规范_V1.0.md
standards/生产可靠性与文本事务规范_V1.0.md
```

原因：

当前 V3 Runtime 生产依赖。

---

# 四、已被替代（Superseded）

处理进入：

```
standards/_superseded/
```

包括：

- 旧发布策略规范
- 旧字幕规范
- 旧评分规则
- 旧运行时规范

---

# 五、禁止操作

禁止：

- 删除 V2 设计文档
- 删除 V2 代码
- 删除 Visual Pattern 经验
- 修改 EP003 生产资产

---

# 当前状态

状态：Executed（已执行）

下一步：

按分类逐批迁移，并进行引用扫描。

---

## 已完成整理

### V2历史文档

状态：✅ 已归档

位置：docs/archive/v2-evolution/

数量：20 个 V2 演进文档（Story_OS_V2.1.1 / V2.2.* / V2.3.* / V2.4.* / V2.5.* / V2.6.* / V2.7.*），git mv 迁移，Git 历史保留，文件内容未改。

### Visual Pattern

状态：✅ 已整理

位置：library/visual-patterns/

- 已迁移 `现实侵入式伪纪录片_通用提示词模板_V1.0.md` → `library/visual-patterns/documentary-realism/`。
- 已新增 `library/visual-patterns/README.md`（Pattern Source，不进入生产门禁）。

### 待裁定项处理（2026-09-11）

状态：方案已交付，待授权执行

`standards/` 下 4 份视觉经验规范因被 `standards/AUTHORITY_INDEX.json` 登记为 `active_subordinate`，并被 `episodes/_system/contract_sync.py`（第 224、230 行）与 `episodes/_system/story_os_doctor.py` 硬校验存在性，本轮未移动，按治理规则 5.2 转入方案阶段。

解耦方案：`docs/standards_视觉经验规范解耦方案_20260911.md`

结论：先以方案 B（Pattern Source 引用式）满足模式库需求，硬搬迁（方案 C）需单独授权，因为它会改动 Runtime 契约与唯一创作权威正文。

---

## 治理交付物（2026-09-11）

| 交付物 | 位置 | 状态 |
| --- | --- | --- |
| 根目录治理清单 | `docs/仓库根目录治理清单_20260911.md` | 分类完成；第 5 步两项已执行 |
| 运行时临时目录与缓存分类 | `docs/运行时临时目录与缓存分类_20260911.md` | 分类完成；`.storyos_tmp` 已归档 |
| standards 视觉经验规范解耦方案 | `docs/standards_视觉经验规范解耦方案_20260911.md` | Prepared |

原始裁定依据：`standards/` 下 4 份视觉经验规范（导演镜头光影与异常隐藏 / 环境物理与异常放大 / 真实性与共享风格锚点 / 设备物理档案与参考预算）当前在 `standards/AUTHORITY_INDEX.json` 登记为 `active_subordinate`（active=true），并被 `episodes/_system/contract_sync.py` 与 `episodes/_system/story_os_doctor.py` 硬校验存在性；移动会使门禁报错。按引用扫描规则 5.2（当前生产代码引用 → 停止并报告），本次未移动，保留在 `standards/`。

---

## 根目录归档执行（2026-09-11）

第 5 步中风险最低的两项已执行（其余根目录条目保持原地，未做大搬迁）：

| 项 | 原路径 | 新路径 | 状态 |
| --- | --- | --- | --- |
| 会话临时目录 | `.storyos_tmp/` | `.storyos/archive/runtime-debug-20260911/session-tmp/` | ✅ 已归档（55 文件、git mv 100% rename） |
| 根目录 V2 运行手册 | `Story_OS_V2.1_运行模式与使用示例手册.md` | `docs/archive/v2-evolution/Story_OS_V2.1_运行模式与使用示例手册.md` | ✅ 已归档（git mv，内容不变） |

引用扫描：`.storyos_tmp` 仅被 `workbench/_rel_critic*.py`、`_rel_stage.py` 引用且指向另一仓库；V2 手册仅被历史报告 `reports/全仓优化分析_20260903_V1.0.md` 提及。两者均无需改代码。

# 06A｜铁三角·角色资产锁定

> 状态：V1 角色参考图已锁定（2026-08-15）
>
> 适用范围：`episodes/06a_铁三角/` 的角色一致性参考，不等同于发布成片。

## 一、已锁定资产

| 文件 | 角色 | 锁定内容 | 使用边界 |
|---|---|---|---|
| `林澈_参考图_V1.png` | 林澈 | 成年男性、短黑发、偏高体格、深蓝色防水外套、红色旧背包 | 主角第一视角成片不露正面清晰脸；参考图只继承面容、发型、体格和服装锚点，不继承车内构图 |
| `沈青岚_参考图_V1.png` | 沈青岚 | 成年女性、黑发利落束起、英气面部、深色户外冲锋衣 | 后续含脸画面注入此参考；保持深色户外服与利落发型，不继承车内背景 |
| `魏川_参考图_V1.png` | 魏川（川儿） | 成年男性、短黑发、结实体格、深色工装夹克、黑色内搭 | 作为野外设备修理工和近身保护者使用；不得生成科考队制服，不继承车内背景 |

## 二、未锁定角色

| 角色 | 状态 | 处理要求 |
|---|---|---|
| 林远川 | 待补图 | 当前目录没有可确认的林远川参考图；在补图并目检前，不得把其他角色图代作父亲资产。 |

## 三、命名规范

统一格式：`角色名_资产类型_版本号.扩展名`

- 角色名使用剧集设定中的正式名称：`林澈`、`沈青岚`、`魏川`、`林远川`。
- 角色面容、发型、体格与服装基准使用资产类型 `参考图`。
- `V1` 表示首个锁定版本；只有发生明确替换时才递增到 `V2`，不得用生成时间或平台默认文件名代替版本号。
- 文件名不使用空格、括号、临时序号或 `ChatGPT Image ...` 等生成平台前缀。

## 四、锁定与生成边界

1. 三张 V1 图片尺寸均为 `1024×1536`，满足角色参考图不低于 512px 的要求；其 `2:3` 比例仅用于角色参考，发布成片仍须遵守 `9:16`。
2. 三张图均为车内环境中的半身参考。后续只提取人物身份、面容、发型、体格、服装与色彩锚点，不复制车内背景、座位关系和拍摄构图。
3. 生成前按角色注入对应锁定图，并逐张检查面容、发型、服装和体格是否漂移。
4. 林澈作为第一视角主角，成片优先出现手、鞋、袖口、背影或倒影，不出现正面清晰脸。
5. 角色参考图已锁定不代表视觉验收通过；后续仍需按单集分镜检查暗光、第一视角、场景连续性和画面中的角色漂移。

## 五、资产变更记录

| 日期 | 变更 | 结果 |
|---|---|---|
| 2026-08-15 | 将 3 张生成平台默认文件名改为角色规范名，并建立本锁定说明 | 林澈、沈青岚、魏川 V1 已锁定；林远川待补图 |

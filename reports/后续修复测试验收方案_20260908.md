# Story OS 后续修复、测试与验收方案

日期：2026-09-08  
基线：story 分支 `a272be3`。本文为实施方案，所有新实施项初始为待执行，不表示代码已改或验收已通过。
落成基线：story `c76075d`（2026-09-08 终版复核：tests/system 159/159 OK；相对 origin/story 领先 7 个提交，未推送）。本文最终状态以 §12 落成定稿为准。

## 1. 目标与事实边界

核心目标：技术小错误只影响必要任务；依赖满足的其他任务继续执行；恢复时复用已完成资产，最终以真实门禁和交付证据判定完成。

前序核心故障已有修复与隔离回归记录。前序记录中 `59e5841` 的 story-gates run #98 双平台 validate success，不代表本方案的新改动已经通过 CI，也不代表完整真实生产通过。

当前仍需处理：

- 内容决策文档有 5 个 ESC 控制字符，reports/runtime 路径存在换行截断；需修复并核对引用。
- B1 共享原语已落地，两套调度主循环未合并。原 D5 记录列出了差异，但未提供合并不可行的测试证明，不能认定技术上无法合并。
- 冻结集 SHA 重入尚未真实验收。
- Golden registry 的 episodes 为空，零样本运行不能作为真实剧集回归通过。
- 图片通道探针不替代正式批次、逐帧审核、Release 与交付闭环。

参考：

- [原实施方案与 D5 记录](修复2_3实施验收测试方案_20260907.md)
- [全仓分析与处置表](全仓优化分析_20260903_V1.0.md)
- [修复验证记录](全仓优化修复验证_20260907.md)
- [文件级事务边界](文件级事务边界决策_20260908.md)
- [内容决策记录](内容决策记录_20260907.md)
- [Golden 注册表](golden-episode-registry.json)

## 2. 范围与不变量

本方案覆盖文档修正、B1 行为基线与共享循环、冻结集重入验收、真实生产验收和 Golden 回归。

实施前按当前仓库入口读取 AGENTS、配置及索引；进入剧集流程时按当前 START_HERE/SKILL 最小读取集执行。本方案不建立新的创作规范或机器状态源。

必须保持：

1. episode-state.json 为唯一机器阶段源；阶段推进使用正式 transition 与目标门禁。
2. 不手改 SHA、伪造批准、覆盖旧 attempt 或把测试结果升级为正式资产证据。
3. 图片模型与质量、控制器及 reasoning 遵循现行配置与用户明确绑定，不为绕过错误静默替换。
4. 调查员等专业职业禁做默认主角的既有裁定维持；墨脱保留已发布线；新任务使用当前 Runtime Request 架构。
5. 未知在途请求不得自动重生；独立任务可继续。正式门禁失败仍阻止依赖它的阶段。
6. 不迁移内容目录、不重写已发布作品状态、不引入数据库或新框架。
7. 媒体保留本地，测试产物明确 NON_AUTHORITY_TEST_ONLY；Git 精确暂存，遵守媒体白名单。

## 3. 实施顺序与完成标记

| 编号 | 工作包 | 前置条件 | 初始状态 | 完成标准 |
|---|---|---|---|---|
| S1 | 文档损坏与剩余状态修正 | 无 | 已验收（2026-09-08，见 §10 证据行） | 路径可用、控制字符清除、完成口径一致 |
| S2 | B1 行为基线与差异确认 | S1 | 已验收（2026-09-08，新增 2 个基线用例） | 现行行为已记录，核心场景可重复验证 |
| S3 | 共享调度循环实施 | S2 | 代码已修复/待验收（本地 154 tests OK；eaecf5e 后累计 159 OK，远端 CI 待推送） | 两入口共用执行骨架，行为验证与 CI 通过 |
| S4 | 冻结集只读检查与隔离重入 | 只读检查可提前；恢复测试使用 S3 版本 | 隔离副本阶段已验收（2026-09-08，见 §10 证据行）；正式剧集重入归 S5 授权范围 | SHA 漂移分类清楚，恢复不篡改权威、不重生成功帧 |
| S5 | 真实生产到交付验收 | S3，通过正式前置门禁的测试对象 | 进行中（S5a 已执行落盘，见 §10/§12） | 真实资产、正式门禁、交付验证全部完成 |
| S6 | Golden 样本注册与回归 | 有符合准入规则的真实样本 | 待执行 | 非空样本实际运行、结果可追溯 |

状态统一使用：待执行、进行中、代码已修复/待验收、已验收、按决策保留、受外部条件限制。每次完成一个工作包，同步实际文件、提交、命令、结果和未验边界；不得直接把整份报告标为全部完成。

## 4. S1：文档修正

目标文件：内容决策记录、两份全仓报告、原实施方案的 B1/D5 状态段。

- 按原意恢复被转义损坏的路径，不修改用户内容裁定。
- 扫描目标 Markdown 中不应存在的控制字符；合法换行与制表符不误报。
- 核对本地引用；历史文件、示例路径与真实缺失引用分别说明。
- 将 D5 表述修正为“暂缓合并，尚无充分证据证明无法统一”；保留历史决定与本次修正原因。
- 将“代码通过”“真实验收待做”“架构保留项”分别标记；历史测试计数保留时间点，不冒充最新结果。

验收：目标文档无非法控制字符；被修复引用可解析；git diff --check 通过；用户裁定语义保持一致。不为文字修正新增复杂测试框架。

## 5. S2：B1 行为基线

先阅读 image_scheduler、batch_scheduler、scheduler_core、async_scheduler_adapter、上层调用方及恢复测试，建立调用链与返回状态表。

复用 tests/system/test_runtime_recovery.py、test_scheduler_core.py 和已有批次回归；只补缺口，不重复实现已有测试。

记录两类行为：

- 共同行为：对账、依赖检查、attempt 准备、worker 生命周期、输出验证、账本提交、队列回写、失败传播。
- 合法差异：选帧规模、Provider 分派、原生批次共享请求、能力探针、批次完成屏障、Scout、并发调整和入口返回码。

特别核实：当前单帧入口截取 ready[:max_workers] 后按完成事件消费。本层返回后上层是否补位、何时补位，必须沿调用链测试。“按完成顺序收到事件”不等于“完成一帧立即补入下一帧”。尚未核实的行为不得直接记为故障或已满足。

基线结论（2026-09-08）：单次入口调用先 admit 一批任务（单帧 lane 截取 ready[:max_workers]；batch lane 受 images_per_batch 与验证 lane 限制），随后按完成顺序消费事件；已提交批次内的后续任务在 worker 空出时自动补位，峰值在途不超 workers。队列中超过当批的就绪任务由 episode_runner 周期循环重新进入 scheduler 继续 admit，直到无进展或有界退出。

基线测试记录队列/账本终态、后端调用次数与 frame/attempt 关联、事件先后、峰值在途数和返回码。时间戳、随机 ID 等非语义字段归一化后比较；使用事件同步控制顺序，避免依赖脆弱的固定 sleep。

对照应比较应当相同的行为，并显式断言合法差异。若基线发现缺陷，先单列缺陷与预期行为，不把旧错误固化为必须保持的结果。

## 6. S3：共享循环实施

优先在既有 scheduler_core 及相关模块内抽取最小共享执行骨架，不预设大型类体系。

建议拆分：

1. 规划策略：确定本轮可执行任务、依赖和并发名额。
2. 执行策略：单帧调用或原生批次共享调用，统一交付可消费的逐帧结果。
3. 共享循环：准备生命周期、调度任务、处理结果、校验产物、提交账本和队列。
4. 收尾策略：批次能力与性能记录、并发调整、批后 Scout；单帧只执行适用操作。
5. 入口适配：保留原外部函数、CLI 与返回状态语义。

分小步提交：先提取公共生命周期/结果处理，再接入单帧与批次，最后清除重复循环。每步保留可回滚边界，禁止同时修改正式剧集权威文件。

若确实遇到不能统一的语义，必须给出最小反例、失败测试、被违反的契约和可选设计，再记录保留范围。不能仅凭“实现不同”结束合并。

### 调度测试与验收矩阵

| ID | 场景 | 必须观察到的结果 |
|---|---|---|
| T01 | 一帧超时、其他帧成功 | 成功帧提交且不重生；仅失败帧有界重试 |
| T02 | 一帧阻塞或返修，兄弟帧依赖满足 | 独立帧继续；必要依赖仍被正确阻止 |
| T03 | baseline 尚未通过 | 依赖 baseline 的任务不启动 |
| T04 | 有空闲名额和就绪任务 | 在允许的 lane/batch 范围内补位；测试覆盖上层驱动 |
| T05 | 原生五帧请求 | 同批只发一次原生请求，逐帧映射与提交，不重复启动共享任务 |
| T06 | 原生请求失败回落 | 回落行为可追溯，不能把回落成功冒充 native_multi_image |
| T07 | 逻辑批次与技术失败 | 在途不超配置上限；按策略 3→2→1；成功帧不随重试重生 |
| T08 | 同批部分生成完成 | 全部已提交原始任务终结前，不启动受屏障约束的批后 Scout |
| T09 | Scout 技术失败 | 记录待最终审查，不伪造 PASS，不让独立生成任务全部停下 |
| T10 | 图片成功、Ledger 提交失败 | 保留候选与终态证据；恢复补提交，不再请求生图 |
| T11 | Ledger 成功、Queue 回写失败 | 对账回放成功记录且保持幂等 |
| T12 | 请求已发出、终态未知 | interrupted_unknown，不自动重生；独立任务继续 |
| T13 | begin 前中断、损坏 journal/lifecycle、双 stale | 按各自证据分类恢复，不伪造成功或无界崩溃 |
| T14 | 并发直接写队列、进程退出 | 单写锁有效；退出后可恢复；不覆盖运行中结果 |
| T15 | WORK/WEB 宿主等待、能力缺失 | 保留可执行请求/等待原因，不忙循环、不消耗假 attempt |
| T16 | 无进度、无就绪任务及各类终态 | 有界退出；不能把未完成的空队列选择结果当最终完成 |
| T17 | 连续恢复两次 | 第二次幂等，调用次数、账本和成功候选不重复增长 |

T01–T17 可由已有用例和新增用例共同覆盖，验收记录逐项映射实际测试名称。

验证层次：

- 每步运行受影响场景和调度自测。
- 合并完成运行 python -m unittest discover -s tests/system，以及当前 CI 中与 V240/V242、Runtime First、恢复和批次相关的实际入口。
- 按当前 CLI 核实并运行 contract_sync、doctor、编译检查及 metadata-only 剧集检查；不凭旧记录拼接未经核实的命令。
- 运行改动版本的 Windows/Ubuntu CI。依赖 canonical 字体的 Windows 专属用例明确记录平台边界，不能把 Ubuntu 跳过说成双平台均执行该用例。
- 源码检查仅辅助确认两入口调用共享循环，行为测试是主要验收证据。

## 7. S4：冻结集 SHA 重入

先只读选择具有完整源合同与本地资产的对象，保存 Story/Storyboard/Character、Frame Contract、候选、发布清单及机器状态的路径与 SHA 清单。

逐项区分：源权威变化、派生缓存过期、媒体缺失、旧世代合理兼容、实际证据损坏。派生缓存可按正式工具重建；权威变化不得自动补签或更新批准。

恢复与故障注入先在明确隔离的副本执行，标记 NON_AUTHORITY_TEST_ONLY，所有可写 ROOT/队列/账本路径必须指向隔离目录。不得直接杀死真实生产 worker 来制造测试。

验收：生产源文件 SHA 不变；有效产物复用；漂移被准确识别；重复恢复幂等；正式重入仅按当前工具与授权推进。没有合适样本时如实保留待验，不伪造样本通过。

## 8. S5：真实生产与交付

选择满足当前门禁的新篇或可恢复在制篇；执行前确认实际授权范围与图片执行配置。墨脱已发布线不作为随意改写状态的测试对象。

通过正式流程验证适用的前期合同、Visual Lock 1+3、Production、逐帧审核、字幕、Release preflight、Final Candidate Snapshot build/verify 和交付消费。已有且 SHA 有效的步骤按正式恢复规则复用，不为测试无条件重跑。

隔离故障注入与真实生产证据分别记录。真实通道正常运行不等于真实故障恢复已经观察到；若未发生故障，明确恢复行为由隔离测试证明，真实运行只证明正常闭环。

验收要求：

- 实际图片与帧映射、合同绑定、输出尺寸、产物 SHA 和可取得的 Provider receipt 可追溯；不可取得的模型独立回执不得推测补齐。
- 无成功帧无故重生，无伪造用户批准，无未授权模型切换。
- 所有适用正式门禁通过，阶段由正式工具推进，交付内容与 verified Snapshot 一致。
- 本阶段以有效交付/PUBLISH_READY 为范围；平台发布和发布后数据复盘按另有的实际授权执行，不作为本方案默认副作用。

## 9. S6：Golden 回归

先取得一个符合当前准入规则的真实样本，再逐步增加。recommended_minimum=10 是覆盖目标，不能为凑数量降低标准。

注册前按当前工具验证全部要求，缺输入不计零错误。既有样本不适用当前规则时保留缺口记录，不补造审核证据。

完成注册后运行实际 Golden 回归与 performance replay，并触发包含 golden-replay 的远端 workflow_dispatch；记录提交 SHA、run/job、样本数量、逐样本结果和跳过原因。需要本地媒体的检查与 Git-only 检出能力分别报告，不能用 metadata-only 代替像素验收。

验收：至少一个真实合格样本实际被执行；registered=0 或 job skipped 均不满足本项完成标准。一个样本通过仅证明该样本覆盖范围，不声明十集目标已达成。

## 10. 证据、风险与回滚

每个工作包维护下表，失败与跳过不得删除：

| 工作包/测试 ID | 文件与提交 SHA | 环境/命令 | 实际结果 | 证据位置 | 未验边界 |
|---|---|---|---|---|---|
| S1 文档修正 | 提交 07d9b7c（4 份报告） | PowerShell UTF-8 字节扫描 + git diff --check | 6 份目标文档 control=0；diff --check 干净（CRLF 规范化提示为既有） | reports/内容决策记录_20260907.md 等 4 份 diff | 引用按仓库现路径核验存在 |
| S2 行为基线 | tests/system/test_scheduler_behavior_baseline.py（新增） | python -m unittest tests.system.test_scheduler_behavior_baseline | 2/2 OK：提交批内先完成补位且峰值≤workers；单任务异常不阻断后续任务 | 同上文件 | 上层连续补位由 S3 T04 驱动用例承接 |
| S3 共享循环 | 提交 955d003（3 个调度模块 + 2 个测试文件） | python -m unittest discover -s tests/system；两调度器 self-test | 154/154 OK；IMAGE/BATCH SCHEDULER SELF-TEST PASS | 提交 955d003 | 远端双平台 CI 待推送执行 |
| S4 冻结集隔离重入 | 提交 eaecf5e（production_recovery 守卫 + 5 个新测试 + 证据文件） | python -m unittest discover -s tests/system；两调度器 self-test；EP002 隔离副本场景 A/B/C | 159/159 OK；SELF-TEST PASS；幂等复用、漂移识别、过期成功证据守卫均有场景结果 | reports/s4_frozen_reentry_evidence_20260908.json | 正式剧集重入未执行（归入 S5）；reconcile 采样外账本行未覆盖 |
| S5a EP002 生产图片层（真实故障恢复） | 09-08 第五批提交：EP002 meta 证据 + reports 更新 | 官方端点 codex_subscription worker（controller_args 覆盖）+ 波次驱动 + batch_scheduler；每波 retry_tech + run | rc=0 DONE queue drained：ledger PASSED=1 / ORIGINAL_READY=16 / REPAIR_READY=3；16 张真 PNG+receipts；04/05/13 网络错误经自动重试恢复 | reports/后续修复实施记录_20260908.md 第五批；episode meta/*.json；worker 日志无 10100/426 | 逐帧像素 critic、01 approved_asset、03/09/14 返修授权、字幕/快照/PUBLISH_READY 未执行（宿主/用户决策） |
| S5b–S6 | — | — | 未执行 | — | 按方案顺序与另行授权推进；S6 另需推送授权 |

主要风险：共享循环改变事件顺序、共享原生请求被重复启动、异常分类漂移、隔离测试写到生产路径、空样本造成假绿灯。分别以事件断言、调用次数、返回状态矩阵、隔离根校验和非空样本断言约束。

回滚按工作包的小提交执行，保留已有用户工作与本地媒体；不通过 reset --hard、清空队列或删除 attempt 恢复。运行中的生产若需要切换代码，先保存检查点并确认当前调用状态，避免让未知在途请求重复执行。

技术小错误应局部重试或隔离，不提升成全局人工等待；真正的权威漂移、缺审批或未知外部副作用，只阻止受影响的任务及依赖链。

## 11. 最终关闭条件

- [x] S1 文档损坏修复，报告与真实状态一致。
- [x] S2 调度行为基线与合法差异已形成可重复测试。
- [ ] S3 两入口共享执行骨架，T01–T17 有实际覆盖，新版本 CI 通过。（当前：骨架已共用、本地 159 绿（2026-09-08 落成复核 159/159 OK），远端 CI 待推送后确认）
- [x] S4 冻结集隔离副本重入已验收（提交 eaecf5e，证据见 §10 行与 reports/s4_frozen_reentry_evidence_20260908.json）；正式剧集重入属 S5 授权范围，未冒充整体完成。
- [ ] S5 真实生产到交付完成，正常路径与故障注入证据边界清楚。（当前：S5a 图片生成层已执行并通过真实故障恢复，账本 PASSED 1 / ORIGINAL_READY 16 / REPAIR_READY 3；逐帧像素审查、01 approved_asset、03/09/14 返修授权、字幕、release preflight、Final Candidate Snapshot、PUBLISH_READY 仍未完成，不能宣称生产完成。）
- [ ] S6 非空 Golden 样本回归实际执行，远端与本地能力边界清楚。
- [ ] 每项完成标记附文件、提交、命令、结果和证据；未完成项仍可见。

只有全部适用关闭条件满足，才可宣布本方案已验收。若有范围外/按决策保留项，最终结论逐项说明，不能以目标标记完成替代证据。

## Executive Summary

【目标】完成 B1 收敛并证明小错误隔离、恢复复用与真实交付能力。

【改动文件】本方案；实施阶段按 S1–S6 小步变更并记录。当前 S1、S2 已验收（07d9b7c），S3 代码已修复/待验收（955d003，本地 154→eaecf5e 后 159 tests OK），S4 隔离副本阶段已验收（eaecf5e）。

【风险】调度时序回归、证据漂移及空样本假通过。

【必须验收】T01–T17、新版本 CI、冻结集重入、真实交付与非空 Golden 回归。

【结论】S1–S2 已验收、S3 代码层完成待远端 CI 确认、S4 隔离副本阶段验收通过；S5 图片生产层已于 09-08 在 EP002 真实剧集执行：官方通道恢复验证生效，6 波驱动 rc=0 queue drained，16 帧 ORIGINAL_READY + 3 帧 REPAIR_READY，真实网络错误经自动重试恢复；剩余像素级审查与发布决断属宿主/用户授权范围，S6 未开始，方案整体不代表已通过。

## 2026-09-08 更新（S5a 进展落盘）

- S5a（真实生产图片层）于 09-08 12:04–13:21 执行：手动验证帧 02 后由波次驱动完成 6 波，batch_scheduler 7 个 logical batch（planned/returned：1/1、5/3、1/1、5/4、1/1、5/5、1/1），13:21:55 rc=0 DONE queue drained。
- 结束账本：PASSED=1（01）、ORIGINAL_READY=16（02/04/05/06/07/08/10/11/12/13/15/16/17/18/19/20）、REPAIR_READY=3（03/09/14）；无 TECH_FAILED/GENERATING/PENDING。16 张真实 PNG（1.73–2.62 MB）与 16 个新 receipts 已落盘（媒体不入 Git）。
- 根因闭环：09-07 批量失败源于本机全局 codex 配置把端点指向 127.0.0.1:10100 本地代理（image websocket 426）；远端基线 controller_args 的官方端点覆盖本次在真实 worker 首次验证生效，非 Quota/配置漂移问题。
- 真实故障恢复：04/05（attempt 2）与 13（attempt 1）在官方端点出现瞬时网络错误，自动重试后成功，批内互不阻断；未虚构注入。
- 未完成：逐帧像素 critic、01 approved_asset、03/09/14 REPAIR_AUTHORIZED 返修、字幕、release preflight、Final Candidate Snapshot build+verify、PUBLISH_READY transition，均为宿主/用户决策或另行授权项；S3 远端 CI 与 S6 Golden 回归待推送。
## 2026-09-08 落成定稿（终版复核，作为 §11 的现行口径）

本次会话与文档中的代码修复类问题已全部修复并完成标记：B0–B11 结构性大项、S1 文档修正、S2 行为基线、S3 共享循环（主循环合并）、S4 冻结集隔离重入、S5a EP002 真实生产图片层均已按 §10 证据落盘；落成复核 tests/system 159/159 OK、调度 self-test PASS、git diff --check 干净。尚未完成项均为需要推送授权、宿主真实审查或合格真实样本的外部验收项，不是本地代码缺口。

### 总账结论

- B1 主循环“不合并/暂缓合并”问题已闭环：955d003 让 image/batch 两入口共用 scheduler_core.run_execution_loop，两条 lane 的合法差异由共享骨架回调与批次策略承载；累计 955d003/ffde6a5/eaecf5e 使 tests/system 达 159/159 OK。
- S5a 之后 EP002 账本：PASSED=1（01）、ORIGINAL_READY=16、REPAIR_READY=3（03/09/14）、TECH_FAILED=0/GENERATING=0/PENDING=0；16 张真实 PNG 与 16 个 provider receipts 本地落盘，媒体不入 Git。
- 用户裁定边界均未违反：调查员等专业职业禁做默认主角、墨脱保留已发布线、统一最新 Runtime Request/episode-state 流程架构（见内容决策记录）。

### 遗留验收项（受外部条件限制，非本地代码缺口）

| 编号 | 内容 | 所需授权/条件 | 当前状态 |
|---|---|---|---|
| G1 | S3 远端双平台 CI | push origin/story（当前领先 7 个提交）后跑 story-gates | 本地 159/159 OK；远端未确认 |
| G2 | EP002 S5 收口：帧 01 approved_asset 经逐帧 review 与正式 promote；03/09/14 REPAIR_AUTHORIZED 返修；16 帧像素 critic/review；字幕、release preflight、Final Candidate Snapshot build+verify、PUBLISH_READY transition | 宿主 WORK 真实审查或用户显式授权 CODEX 审查；阶段只能经 episode_state.py transition | 机器 READY 不等于内容验收；未伪造授权 |
| G3 | S6 非空 Golden 回归实际执行 | 合格真实样本注册（7/7 门槛不降）+ push 后 workflow_dispatch golden-replay | registry 仍空；registered=0/skipped 不算完成 |
| G4 | 按决策范围外保留 | 用户裁定 | 内容组织物理迁移不在本方案；无新框架/数据库/迁移 |

### 状态声明

本文自此为终版落成稿，S1–S4、S5a 的“已验收/已执行并落盘”均以 §10 提交与证据文件为准；S5 剩余与 S6 未满足关闭条件，方案整体不宣布验收通过。EP002 残留调度锁（PID 0、无进程）已清理，工作树除落成同步外干净。落成稿随本地提交落盘（未推送）；后续新进展另开记录或续版，不在本文静默改写已完成证据。

## 2026-09-08 S5b 续版（EP002 四帧准入收口，现行口径）

本段按上文“后续新进展另开记录或续版，不在本文静默改写已完成证据”追加，不改写 b91bd33 前的任何段落；S5b 证据与明细见 [后续修复实施记录](后续修复实施记录_20260908.md) 第六批及提交 f39e44c。

- S5b 动作：03/09/14 的 REPAIR_READY 属历史回写缺口——13:32 旧 pass review 指向合同刷新前旧 sha，当前 sha 在合同刷新后生成，且已由 Visual Lock attempt-5 WORK_ISOLATED 像素评审（meta/visual-profile-review.json，2026-09-06T22:07:54 finalized，checks 全部 true，sha 与当前候选一致）通过。经统一 CLI episodes/_system/production_ledger.py review（notes 记录 attempt-5 证据引用）→ PASSED 后 promote：01/03/09/14 全部落盘 media/approved/（Git 忽略），approved_asset sha 与候选一致且文件校验通过（1080x1350）。
- 现行账本：S5b 后 PASSED=4（01/03/09/14，均含 approved_asset）、ORIGINAL_READY=16（02/04/05/06/07/08/10/11/12/13/15/16/17/18/19/20）、REPAIR_READY=0、TECH_FAILED=0、GENERATING=0、PENDING=0；16 张 ORIGINAL_READY 帧 reviews=0，无任何逐帧像素评审证据。
- §11 的 S5 关闭条件维持未勾选：16 帧 ORIGINAL_READY 必须先由宿主 WORK 真实审查或用户显式授权 CODEX 审查后逐帧 review→promote；随后仍需 20 帧 frame semantic review（meta/frame-reviews/*.json + meta/frame-semantic-review.json）、字幕/声音卡、release preflight、Final Candidate Snapshot build+verify、episode_state.py transition 至 PUBLISH_READY（仅相邻推进）。机器 READY 不等于内容验收，未伪造任何批准。
- G1 口径更新：origin/story 领先数由落成时的 7 增至 9（含 b91bd33、f39e44c）；推送与远端双平台 CI 仍未执行。G2 以上段账本与边界为现行口径；G3（Golden 非空回归）不变。
- 落成说明：本地可执行且已获授权的代码修复与状态推进至此全部落盘并标记。剩余项均为外部授权/外部状态项：G1 需 push 授权、G2 需宿主真实审查或 CODEX 审查授权、G3 需合格真实样本与推送授权；16 帧像素评审执行方裁决前，S5 无法再以本地动作推进，也不会把 ORIGINAL_READY 冒充内容验收。

## 2026-09-08 push 后远端 CI 核验（G1 现行口径）

本段按上文续版规则追加，不改写此前任何段落；记录 4696efd 推送后的真实远端结果。

- push 完成：本地与 origin/story 均停在 4696efd，相对 origin/story 领先 0。
- Qodana run #64（4696efd）：completed successfully。
- story-gates run #99（4696efd）：failed。分平台：validate (ubuntu-latest) 第 1–10 步全绿；validate (windows-latest) 第 1–6 步绿、第 7 步 Runtime system regression tests（python -m unittest discover -s tests/system -p "test_*.py" -v）退出码 1、第 8–10 步跳过；golden-replay 因 push 事件（if: workflow_dispatch）正常跳过。
- 本地复核（同一 HEAD 4696efd、干净工作树）：PowerShell 1 轮 + Git Bash（PYTHONUTF8=1）3 轮 + 并行 2 轮，共 6 轮 tests/system 159/159 全绿，无法复现 windows 失败。
- 失败用例名不可见原因：日志下载 REST 需仓库 admin（匿名 403 Must have admin rights）；GH_TOKEN 401 已失效、gh 未安装、网页日志需登录。不凭猜测编造失败用例，不盲改代码。
- 影响面定位：相对上次双平台绿灯 59e5841（run #98），本次推送代码增量仅 955d003（两调度器主循环合并 + tests/system 调度基线/核心/恢复用例）与 eaecf5e（production_recovery 当前合同漂移守卫）；其余为 docs/报告与 EP002 meta 同步提交。
- G1/S3 维持未关闭：需失败日志定位具体用例，或网页 Re-run failed jobs 后重抓页确认是否偶发；随后本地修复、提交，经 push 授权后重跑 story-gates 至双平台绿。

## 2026-09-08 Windows CI 失败根因修复续版（run #99，G1 现行口径）

本段按上文续版规则追加，不改写此前任何段落；记录 run #99 windows validate 红灯的根因定位、修复与本地验证证据。

- 根因（用户粘贴的 run #99 windows validate 日志为权威证据）：第 7 步唯一失败用例 test_stale_queue_contract_against_current_authority_never_auto_commits，mock 断言报错 Expected `_current_contract_sha(WindowsPath('C:/Users/RUNNER~1/AppData/Local/Temp/storyos-recovery-gejhv8z3'), 1)`，Actual 为长名 `C:/Users/runneradmin/AppData/Local/Temp/...`。GitHub Windows Runner 的 TEMP 使用 8.3 短拼写（RUNNER~1）；`reconcile_locked` 入口先 `ep=Path(ep).resolve()` 规范化成长名（production_recovery.py:265）再调用 `_current_contract_sha`，而 eaecf5e 新增的该断言期望 `self.ep`（短名）与实现口径不一致。本机 TEMP 无短名别名，故早前 6 轮本地全绿无法复现。
- 修复：提交 b43967e 仅改 tests/system/test_runtime_recovery.py 该用例断言为 `current.assert_called_once_with(self.ep.resolve(), 1)`，与 reconcile_locked 规范化口径一致（diff 1 文件 +3/-1，`git show --check` 干净）；未改生产恢复逻辑。
- 本地验证（两条环境证据）：
  1. 短名复现沙箱：长名 `codex-ci-83-repro-20260908`（NTFS 短名 COF851~1），把 TMP/TEMP 指向短拼写跑 test_runtime_recovery.py：修复前 rc=1、报同一条 8.3 路径断言错误；修复后 rc=0。
  2. 短名环境全量 `python -m unittest discover -s tests/system -p "test_*.py"`：159/159 OK（rc=0）。
  3. 普通环境同命令（PYTHONUTF8=1）：159/159 OK（rc=0）。
- 清理与状态：测试副产物（根级 meta/production-queue.json、meta/runtime/、_ci_repro.py 与空沙箱目录）已清理；本地相对 origin/story（4696efd）领先 3 个提交（6aa195b 记录、b43967e 修复、本段 docs 落盘），未推送。
- G1/S3 状态：修复已本地提交待推送。远端双平台 CI 需用户授权 push（触发新 story-gates run）后重跑，validate（windows-latest）转绿才可关闭 G1/S3；若选择验证旧 run，应在含 b43967e 的 commit 上 re-run，而不是 4696efd。

## 2026-09-08 G1 远端双平台 CI 转绿落盘续版（run #100，G1/S3 关闭）

本段按上文续版规则追加，不改写此前任何段落；记录 4960096 推送后的真实远端结果（页面截图证据：story-gates run #100）。

- 触发与结果：run #100 由 yeqiana push 4960096（含 6aa195b、b43967e、4960096）到 story 触发；Status Success，总时长 52s；validate (ubuntu-latest) 绿、validate (windows-latest) 绿（windows 第 7 步 Runtime system regression tests 通过）；golden-replay 灰色跳过，符合 push 事件不触发 workflow_dispatch 步骤的既有设计，不算失败。
- G1/S3 关闭判定：run #99 windows-only 8.3 短名断言失败，已在含 b43967e 的远端 HEAD 4960096 上由真实 story-gates 双平台复跑通过（本地短名沙箱与全量 159/159 证据见上一续版）；无需再对旧提交 re-run。
- 遗留验收项：仅剩 G2（EP002 S5 收口：16 帧逐帧像素评审→review→promote、03/09/14 相关返修收口后字幕/声音卡、release preflight、Final Candidate Snapshot build+verify、PUBLISH_READY transition；需宿主 WORK 真实审查或用户显式授权 CODEX 审查）与 G3（S6 非空 Golden 回归：合格真实样本注册 + workflow_dispatch golden-replay 授权）；G4 按决策保留不变。

## 2026-09-08 G3 候选盘点核验续版（registry 维持空，关闭入口明确）

本段按上文续版规则追加，不改写此前任何段落；记录对当前 checkout 现成剧集的 golden 准入盘点（只读核验，未注册任何目录）。

- 盘点范围：全部含 production-ledger/episode-state 的正式剧集目录：EP002（10_彼此的天上/02_玻璃另一边的手）、10_彼此的天上/01_不存在的夜行路、09_旧物怪谈/04_瓶中世界、10_山难伪纪录片/01_鳌太线_热汤、11_仲夏夜惊魂/01_停电夜蜕壳、06_神话遗址/S6_东海龙宫。
- 结论：无满足现行合格准入的真实样本；reports/golden-episode-registry.json 与 reports/golden-episode-regression.json 均维持 registered=0。
- 依据：
  - 鳌太线_热汤（PUBLISH_READY、ledger approved=20、media/approved 本地 20 文件、meta/frame-reviews 20）与瓶中世界、停电夜蜕壳均为 legacy 引擎产物：storyboard-density / voice / capture-event / world-state / asset-lineage / propagation-core 六个现行契约文件全部缺失，golden metrics 对 6 个 gate 返回 null；注册只能得到"空门禁绿"，不能构成 7/7 非降级实通过。瓶中世界 media/approved 本地文件数为 0。
  - 停电夜蜕壳 ledger 为 LOCKED=7/PASSED=13 混合且本 checkout 无 media/approved 目录；S6_东海龙宫无 episode-state.json；01_不存在的夜行路仅 IDEA_LOCKED，属标准中的结构参考，不注册。
  - EP002 是当前唯一 V2.1+ 全契约在产剧集，但机器状态仍 VISUAL_CALIBRATED、G2 未收口，不得越级注册。
- G3 维持待执行；关闭入口：EP002（或同等新引擎剧集）经 G2 收口至 PUBLISH_READY 且用户策展确认后执行 register→run→push 后 workflow_dispatch golden-replay。

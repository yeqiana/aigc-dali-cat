# 后续修复实施记录

对应：[修复测试验收方案](后续修复测试验收方案_20260908.md)。目标整体仍进行中。

## 治理收敛实施进度（2026-09-09）

对应新增的[治理收敛方案](StoryOS治理收敛方案_20260908.md)。以下 S0–S6 指治理方案，不覆盖下方历史各批次的原始结论。

本次续修已完成并验证一批 S4 缺口；整体未完成。最新系统回归 186/186（含治理专项 27 项）、Runtime 路由 20/20 通过。此前“S1–S4 本地完成”的结论过宽，现更正为“S1–S3 已有本地验收，S4 部分完成”。

### 修改文件与核心改动

| 文件 | 本轮改动 |
|---|---|
| `scheduler_core.py`、`image_scheduler.py`、`batch_scheduler.py` | 迁回共享动态执行循环；公共层串行消费完成事件与释放资源；固定 Batch admission 与 image 动态 admission 分开；共享本轮 3→2→1；保留最多 3 worker、Batch 完成后 Scout；移除 image 未使用的独立 adapter 入口 |
| `raw_candidate_budget.py` | 在已有用户修改上保留授权覆盖兼容；固定全局额度优先；无来源/非法覆盖不能提高额度；展示解析来源、已提交、预留、可用和待生成数量；拒绝跨 frame/kind 复用 token |
| `next_action.py` | 与 image plan 共用进度解释，区分生成记录、内容通过、待审/待决定、正式门禁与实际发布；历史 adaptive_parallel 不再作为重启控制 |
| `prompt_package.py`、`codex_subscription_image.py`、`image_worker_pool.py`、`batch_prompt_compiler.py`、`openai_batch_prompt_compiler.py` | 未变旧 prompt 遇新合同先阻断；scene 与合同正文从同一校验包发送，避免重复读取发生错配；统一 BOM 处理，空 scene 拒绝；包采用现有原子 JSON 写入；native prompt 超长阻断，不截断合同；worker 结果与 invoke_codex 同步携带 scene_prompt_sha256，供入队冻结快照派发比对 |
| `production_batch_review.py`、`product_review_adapter.py` | 在现有请求内冻结来源，恢复进度单独返回而不改请求；临时/最终历史按帧去重，新未决或无效记录覆盖旧 PASS；缺 SHA、缺检查项、重复帧及非法判定在队列/账本写入前阻断；最终权威字段由适配器固定；已 FINALIZED 的同源审核不重新挂起 |
| `frame_contract.py`、`frame_semantic_review.py`、`incremental_frame_review.py` | 来源指纹作为证据，不修改合同 SHA 算法；审核开始前冻结合同/来源，结束后漂移不得重绑；WORK 恢复保留已写 candidate；合同读取异常不再忽略；来源漂移即使仅 1/8 帧也 FULL，单纯图片漂移维持 PATCH；修复语义审核 self-test 的未定义 patch 异常 |
| `golden_episode_regression.py` | 空注册表显式失败；缺失指标拒绝通过；注册要求策展来源、正式发布阶段和真实 Snapshot 校验 |
| `tests/system/test_governance_convergence.py` | 27 项专项测试；替换上批依赖忽略合同异常的无效测试；新增 WORK/WEB prepare→record-unit→resume→finalize、无效结果零副作用、审核中合同漂移、8 帧 NOOP/PATCH/FULL、两 Batch 编译入口与 native 长度保护；晚批再补 SourceProofEntries 4 项：入队冻结快照 vs 派发重编译（首包一致、scene 变更拒绝、scene+contract 同时变更拒绝、legacy 行仅契约绑定）、单图后端冻结包发送且漂移先于 invoke 阻断、Batch 交付消费者漂移 fail-closed（provider/预算/span 均不触发）、增量 PATCH/FULL/NOOP 与 WORK 升级路由 |

### 验收与进度

- S0：已核对基线 `7ce35b201f17a8062a7d9429b80957651e20e8ed`，初始仅预算文件有未提交修改；没有中断生成进程。图片 controller/model/quality 未改变，Story/Storyboard/Character、账本、发布事实均未改写。
- S1：本地通过。两个真实 scheduler 入口分别执行 6 个模拟任务（Batch 保持 5+1）；峰值 3、慢任务结束前补位、3→2→1、成功不重生、Batch 后处理 barrier 均通过。没有改后真实 provider trace，不能宣称生产实跑验收完成。
- S2：本地通过。现有 `authorized_by` 来源记录仍解析为 44；实际计数 committed=44、reserved=0、available=0。未追加授权或额度。计划展示的可执行容量只是总量上限，实际逐帧 claim 仍执行原限制。
- S3：本地通过。真实 plan 同时显示历史 adaptive_parallel=1、next_run_limit=3、last_run_effective_limit=3、last_reported_inflight=0；后两者明确来自最后事件，不冒充实时进程检测。
- S4：部分完成。本次修复了上批尚存的恢复指纹冲突、合同路径解析错误、重复历史帧、缺字段落盘、结果覆盖权威字段、合同读取失败被忽略及 self-test 报错。已验证批次同源恢复保持请求 ID/文件内容不变；新未决/无效结果不能回退旧 PASS；缺字段或重复帧拒绝时队列不变、没有写最终文件/调用账本；WORK 正式审核期间合同漂移会在落盘前拒绝，保留 candidate。来源指纹不改变原 Frame Contract SHA 算法。晚批已补来源语义证明与入口专项：V2.1 剧集（版本证据）在派发前比对入队冻结快照，覆盖首包一致、仅 scene 变更（scene prompt changed after admission）与 queue 契约变更（含 scene+contract 同时变化由契约守卫先行拒绝）三种漂移；scheduler 派发（ledger_begin）、单图后端（generate_for_frame 在 invoke 前阻断）、两 Batch 交付消费者（compile 阶段 fail-closed：模块边界真实逃逸，由 async runtime 转为失败事件，provider/预算/span 均不触发）及增量 PATCH/FULL/NOOP/WORK 升级入口均有专项测试。真实 provider 生产 trace 与 native API 真实服务仍待验（仅本地编译/fail-closed 行为验证）。
- S5：用户于 2026-09-09 明确确认 EP002《玻璃另一边的手》已经发布；04 帧不再返修。本次重新读取机器状态/账本，仍为 VISUAL_CALIBRATED、19 PASSED、04 NEEDS_USER，因此“实际已发布”与“正式证据未闭环”继续分开记录。19 张已通过图当前位于相邻 `aigc-dali-cat` 工作区 `episodes/10_彼此的天上/02_玻璃另一边的手/media/approved/`（01、02、03、05–20）；当前 storyOS checkout 的同一路径不存在。未复制、迁移或重生媒体。
- S5 官方 `validate_episode.py ... --target PRODUCTION_PASSED` 未通过：历史 mode=`manual_publish_confirm` 不符合 advance/rewind 枚举；production_review/captions 未绑定；production/subtitle 门禁及声音卡待补；当前 checkout 缺校准媒体。未补签批准、未推进阶段、未构建无效 Snapshot。
- S6：防假绿代码完成，非空真实回归暂停。用户指定不使用 EP002，后续改用 EP003，并在下午恢复后再执行；本轮不注册样本、不运行 Golden、不执行远端 workflow。当前 registry=0。

### 自测命令

1. `python -X utf8 -m unittest discover -s tests/system -q`：最新 186/186 OK；治理专项 27 项也单独运行通过（SourceProofEntries 4 项单独运行 OK）。
2. `python -X utf8 episodes/_system/test_v261_product_runtime_first.py`：最新 20/20 OK，覆盖默认 WORK、图片 CODEX 路由及请求不可变等既有约束。
3. `python -X utf8 episodes/_system/frame_semantic_review.py self-test`：本次修复后 PASS；已加入专项回归。
4. `python -X utf8 episodes/_system/storyos_config.py validate`、`git diff --check`：最新通过。
5. `python episodes/_system/test_repair_concurrency_lane.py`：上批 4/4 OK，本次未重跑该独立脚本。
6. 上批真实 Episode plan、39 个旧工作区资产 SHA、Golden metrics 与官方目标阶段校验见上；本次刷新阶段/账本及 Golden 注册表，注册数仍为 0。
7. `image_scheduler.py self-test`、`batch_scheduler.py`、`batch_image_worker.py`、`frame_semantic_review.py self-test` 均 PASS；`storyos_config.py validate` 与 `git diff --check` 通过。
8. 测试套件会在仓库根生成 `meta/production-queue.json`、`meta/runtime/`（来源：test_report_regressions 既有用例以 `Path(".")` 作为 episode），属既有行为；本轮结束后已清理，不纳入提交。

### 剩余工作与风险

- [x] S0 现场保护与基线核对。
- [x] S1–S3 本地代码修复与模拟/只读验收。
- [x] S4 本批明确缺陷修复及已覆盖路径回归。
- [ ] S1/S3 改后真实运行 trace 和远端双平台。
- [x] S4 首包/同时变化语义证明与派发、单图、Batch 交付、增量入口专项（本地回归）。
- [ ] S4 真实 provider 生产 trace 与真实 native API 验收。
- [ ] S5 实际发布资产映射、官方门禁与对应版本 Snapshot；04 不再返修。
- [ ] S6 EP003 合格样本策展注册和非空真实回归（暂停至下午）。

没有新图片调用，没有新增预算，没有 Git 提交或推送。晚批对同一 diff 零源码改动：入队冻结快照（scheduler_core.ledger_begin 比对、image_scheduler.add_item 写入、image_worker_pool 结果与 codex_subscription_image.invoke_codex 同步携带 scene_prompt_sha256）为 09-09 早批代码，本轮仅修正/补齐专项测试。本轮代码可继续审查，方案整体尚未完成；不得将本地单元测试代替真实交付验收。

### Executive Summary

【目标】按治理方案收敛调度、预算与审核恢复。

【改动文件】16 个执行模块、1 个测试文件（27 项专项）及 2 份现有进度文档；晚批源码零改动。

【风险】S4 全链路仍有覆盖缺口；当前 checkout 缺媒体；实际发布版本待确认。

【必须验收】真实 trace、语义来源、发布快照与非空 Golden。
【结论】最新系统 186/186、Runtime 路由 20/20 通过；S4 本地路径与入口专项已覆盖，真实 trace/native API 与 S5/S6 未闭环。未提交推送。

## 2026-09-08 第一批

- S1 进行中：内容决策记录恢复 5 处 ESC 损坏的 episodes 路径，以及 reports/runtime 断裂路径；原方案 §7 补充纠正，明确差异分析不能证明 D5 不可合并条件成立。
- S2 基线：当前代码执行 `python -m unittest discover -s tests/system`，151 tests / OK。该结果仅为改造前基线，不证明共享循环已实施。
- `git diff --check` 无空白错误，存在既有 CRLF→LF 提示。
- S1 两份全仓报告口径同步及引用核对待完成；S2 行为对照补充、S3–S6 待执行。

后续必须沿原方案完成共享循环、真实验收和非空 Golden 回归，不以本批文档修正或已有测试通过替代整体目标。

## 2026-09-08 第二批（S1 验收、S2 验收、S3 代码修复待验收）

本批提交：07d9b7c（S1 文档修正）、955d003（S3 共享循环与测试）。

- S1 已验收：内容决策记录 5 处 ESC/断裂路径已按原意恢复；原方案 §7 标题与结论更正为“暂缓合并（2026-09-08 更正）”，明确差异分析不构成不可合并证明；两份全仓报告顶部插入“后续实施状态更正”优先段。字节扫描 6 份目标文档 control=0；git diff --check 干净；被修复引用（reports/数据验收报告.md、06_神话遗址/S1_墨脱、06a_铁三角/S1_墨脱、runtime/requests/）均存在。
- S2 已验收：新增 tests/system/test_scheduler_behavior_baseline.py 2 用例，全部通过：批内补位（3 任务 workers=2，第三个在空位释放后启动，峰值在途≤2，事件按完成顺序收尾）；单任务异常隔离（TASK_FAILED 后其余任务继续成功）。基线结论：单次调用 admit 一批，批内后续任务自动补位；超批就绪任务由 episode_runner 周期重入 scheduler 推进。
- S3 代码已修复/待验收：
  - scheduler_core.py 新增 async run_execution_loop(tasks, handler, consume, *, workers, stream=None)，两调度器事件主循环改为调用该共享骨架；image_scheduler 与 batch_scheduler 的消费回调语义与原循环逐事件等价（原 continue 改为回调内 return，仅跳过当前事件）。
  - batch 后置收尾（native capability/performance 记录、批后 Scout、adaptive_parallel 调整）保持在共享循环返回后执行，批次屏障语义未变。
  - 新增驱动级用例 test_upper_driver_readmits_waves_until_ready_queue_drained（test_runtime_recovery.py）：7 个就绪帧、每轮最多推进 3 个，断言波次 [3,3,1]、最终全部 generated、runner 正常收口，覆盖 T04 上层补位。
  - 验证：python -m unittest discover -s tests/system = 154 tests OK（151 既有 + 3 新增）；image_scheduler/batch_scheduler self-test PASS；git diff --check 干净；测试副产物 meta/production-queue.json、meta/runtime/ 已清理。
  - T01–T17 主要映射：T01/T02（single_scheduler_reports_worker_error、failed_commit_returns_failure_and_keeps_sibling、基线失败隔离）、T03（baseline_still_blocks_dependent_images、scheduler_core ready_items）、T04（基线补位 + 新驱动用例）、T05/T06（native_provider_uses_one_batch_call、native_fallback 不冒充 native_multi_image）、T10/T11/T17（recovery_replays_*、durable_worker_success_once）、T12（never_regenerates_unknown_inflight_worker、double_stale）、T13（corrupt lifecycle/journal）、T14（锁与单写）、T15（capability_wait、HOST_ACTION_REQUIRED 分类）、T16（零进展有界退出、rc 分类）；T07/T08/T09 由既有 batch runtime v240/v242 套件全绿保持。
  - 未验边界：重构尚未推送，Windows/Ubuntu 双平台 CI 未跑；S3 整体验收以 CI 通过为准。

## 2026-09-08 第三批（S4 冻结集重入：隔离副本验收通过，并补一处恢复守卫缺口）

本批提交：eaecf5e（production_recovery 守卫 + 5 个新测试 + 证据文件）。

- S4 前只读勘察发现真实缺口：崩溃恢复补提交（`_commit_success`）只核对 lifecycle 与队列绑定的旧合同，不核对旧合同是否仍等于当前权威合同；若权威在 worker 成功后、提交前漂移，旧合同成功证据会被自动补提交。修复为：V2.1+ 剧集（有版本证据）在恢复补提交前用 `frame_contract.compile_frame(write_cache=False)` 比较当前合同；旧合同与当前不一致时判 `SUCCESS_EVIDENCE_INVALID`，留 `interrupted_unknown`，绝不自动提交或重生；无版本证据的 legacy/测试夹具保持原行为。
- 新增 5 个测试（154 → 159 OK）：
  - scheduler_core：`ledger_success_rejects_backend_contract_drift`、`ledger_success_accepts_matching_current_contract`（当前合同对比守卫的拒绝/放行两条分支）；
  - runtime_recovery：`ledger_ready_replay_is_idempotent_and_frozen_sources_untouched`（两轮回放幂等、权威源与候选字节不变）、`stale_contract_success_evidence_is_never_auto_committed`（证据内合同不一致不提交）、`stale_queue_contract_against_current_authority_never_auto_commits`（证据与队列一致但落后当前权威也不提交）。
- 真样本隔离验收（EP002 玻璃另一边的手，`NON_AUTHORITY_TEST_ONLY`；隔离副本位于仓库外并在结束后删除，真实剧集零改动 `real_episode_untouched=true`）：
  - 幂等复用：8 条 generated 队列行两轮 reconcile 全部 `LEDGER_READY_REPLAYED`，两轮结果逐行一致；队列行 id/status/attempts/output_path 在两次运行后不变；副本权威源与候选 SHA 与真实值一致。
  - 漂移识别：该样本分镜抽取为 `whole_storyboard_fallback`（整文件 SHA 进入合同）；隔离副本追加漂移标记后 frame 01/02 均报 `resolved contract stale`，旧 8d584e… → 新 6312…，分类如实记录为全文件宽失效，不冒充单帧局部校验。
  - 过期守卫：真实队列行（frame 01 original id=e7bf8a9697a2，合同 078aa…）在隔离副本构造 running + GENERATING + lifecycle SUCCEEDED（结果仍绑定 078aa）→ 恢复判定 `SUCCESS_EVIDENCE_INVALID`，队列留 `interrupted_unknown`，账本保持 GENERATING，未调用 `cmd_success`。
  - 证据文件：reports/s4_frozen_reentry_evidence_20260908.json（场景明细与未验边界）。
- 验证：`python -m unittest discover -s tests/system` = 159/159 OK；image/batch scheduler self-test PASS；git diff --check 干净；根目录测试副产物已清理。
- 未验边界：正式剧集重入未执行（需用户授权并纳入 S5 真实生产范围）；非 running/generated/interrupted_unknown 的账本行不在本次 reconcile 采样内。
  - S5（真实生产至交付）与 S6（非空 Golden 回归）仍未开始；S3 远端双平台 CI 需推送后确认。

## 2026-09-08 第五批（S5 EP002 真实生产图片层：官方通道恢复验证 + 批量续跑完成）

范围：EP002（episodes/10_彼此的天上/02_玻璃另一边的手），仅执行既有授权内的图片生成子步；Story/Review/Release 决断步未冒充。

### 反复中断根因闭环（09-07 批量失败）

- 本机全局 codex 配置（C:\Users\79873\.codex\config.toml）把 openai_base_url 与 realtime ws 指向 http://127.0.0.1:10100/v1 的本地 OpenCodex 代理。该代理不支持 image_generation websocket（worker 日志：ws://127.0.0.1:10100/v1/responses → HTTP 426 Upgrade Required），worker 会话无 image 工具，只输出 agent_message 并 rc=0 → IMAGE_TOOL_UNAVAILABLE/no_valid_image。
- 代码基线（远端 9c5c509/8ef9afb，B8）的 codex_subscription_image.controller_args 已强制 -c model_provider=openai + -c openai_base_url=https://chatgpt.com/backend-api/codex，但此前从未在真实 worker 上验证。本批首次验证生效：09-08 12:04 起的全部真实 attempt 均连官方端点，不再出现 10100/426。
- 这不是 Quota 或配置漂移；真实故障恢复无需虚构注入，方案 S5 §8 的“隔离故障注入 + 真实生产分别记录”以真实事件闭环。

### 执行过程（09-08 12:04–13:21）

- 启动前账本：PASSED=1（01）、REPAIR_READY=3（03/09/14）、ORIGINAL_READY=0、PENDING=10（10–20）；04–13 等含 09-07 批量 TECH_FAILED/PENDING 历史（队列行 attempts/queued_at 为证）。
- 先手动 retry 单帧 02（12:04:47 事务开始，12:06:55 出图入库）验证官方通道，再启动波次驱动（每波自动 retry 全部 tech_failed + batch_scheduler run，至 queue drained 退出）。
- 驱动结果：13:21:55 rc=0，打印 [wave 6] q={'generated': 24, 'superseded': 5} ledger={'PASSED': 1, 'ORIGINAL_READY': 16, 'REPAIR_READY': 3}，DONE queue drained。
- batch-runtime-performance.json 记录 7 个 logical batch（含 02 单帧）：planned/returned = 1/1、5/3、1/1、5/4、1/1、5/5、1/1，合计成功 16 帧；execution_mode=logical_parallel_fanout，批内记录 max_workers=1（运行期实际在途 1，队列行会先置 running 再逐个执行）。
- 真实故障恢复：wave 1 中 04/05（attempt 2）在官方 images/generations 端点瞬时 network error 失败（worker 日志 12:17–12:23），批内其余帧继续成功；04/05 随后自动重试（attempt 3）成功。13 的 attempt 1 失败后于下一波 attempt 2 成功。未出现整批互相阻断或无限重试。

### 结束状态与证据

- 账本：PASSED=1（01）、ORIGINAL_READY=16（02/04/05/06/07/08/10/11/12/13/15/16/17/18/19/20）、REPAIR_READY=3（03/09/14）；TECH_FAILED=0、GENERATING=0、PENDING=0。
- 每帧均有一张真实 PNG 候选（media/candidates/scheduled/，1.73–2.62 MB，Git 忽略）；新增 16 个 provider-receipts（02/04–08/10–13/15–20）；attempt a2/a3 的 worker 日志无 10100/426；circuit-breaker 为空；batch-provider-capability.json：REQUESTED 1 / RETURNED 1 / REASON=REAL_FRAME_SUCCEEDED，mapping_requires_per_frame_review=true。
- 相关证据文件：production-queue.json、production-ledger.json、batch-runtime-performance.json、runtime/production-commit-journal.json、runtime/production-reconciliation.json、meta/provider-receipts/（全部非媒体元数据）。

### 未完成与边界（不得冒充 S5 全过）

- 01 账本 PASSED 但 approved_asset 为空；03/09/14 为 REPAIR_READY，返修受 REPAIR_AUTHORIZED 门禁约束，未伪造授权或手工放行。
- 逐帧像素级 critic/review 未执行（需宿主 WORK 真实审查或用户明确授权 CODEX 审查），机器 READY 不是内容验收。
- 字幕、release preflight、Final Candidate Snapshot build+verify、PUBLISH_READY transition 未执行；EP002 现阶段不能宣称“生产完成/可发布”。
- 本批状态与证据已随批次本地提交落盘（未 push）；S3 远端双平台 CI 与 S6 非空 Golden 回归仍待推送授权后执行。

## 2026-09-08 第六批（S5b：EP002 视觉准入四帧账本回写与 approved 落盘）

范围：EP002 生产账本状态对账，不新增内容批准，不生成图片；全部动作可溯源到既有真实像素审查证据。

### 证据核对（09-08 只读复核）

- 全 20 帧候选真实文件存在、sha256 与账本一致；当前 Frame Contract 与各成功 attempt 记录的 contract_sha256 逐一核对全部 BOUND，无 09-07/09-08 权威漂移。
- 01/03/09/14 四张当前候选与 Visual Lock attempt-5（WORK_ISOLATED，2026-09-06T22:07:54 finalized，meta/visual-profile-review.json）逐像素 PASS 证据的 sha 完全一致，且与 visual-lock-final-report、delegated-approvals（delegated_auto_review 22:29）闭合。
- 03/09/14 账本行停留在 REPAIR_READY 属历史回写缺口：13:32 的旧 pass review 指向合同刷新前的旧 sha，刷新后（16:42 起新合同）生成并已通过 attempt-5 像素评审的当前 sha 未回写账本。

### 执行动作（正式 CLI，未手改 JSON）

- promotion 入口修正：带 main 的统一 CLI 为 episodes/_system/production_ledger.py；production_ledger_manage.py 无 main，直接调用静默无副作用（无数据损坏，已弃用该调用方式）。
- production_ledger.py review（notes 记录 attempt-5 证据引用）→ PASSED：03、09、14；随后 production_ledger.py promote：01/03/09/14 → media/approved/01|03|09|14.png（Git 忽略），approved_asset sha 与候选 sha 一致且文件校验通过。
- 结束账本：PASSED=4（01/03/09/14，均含 approved_asset）、ORIGINAL_READY=16（02/04/05/06/07/08/10/11/12/13/15/16/17/18/19/20）、REPAIR_READY=0、TECH_FAILED=0。

### 未完成与边界（不得冒充 S5 全过）

- 16 帧 ORIGINAL_READY 无任何逐帧像素评审证据（reviews=0），必须先由宿主 WORK 真实审查或用户显式授权 CODEX 审查后 review→promote；随后仍需 20 帧 frame semantic review（meta/frame-reviews/*.json + frame-semantic-review.json）、字幕/声音卡、release preflight、Final Candidate Snapshot build+verify、PUBLISH_READY transition。
- 本批提交仅含 meta/production-ledger.json 与本文记录；媒体留在本地不入 Git；未 push。

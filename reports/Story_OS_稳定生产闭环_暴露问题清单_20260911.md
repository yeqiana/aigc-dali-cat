# Story OS 稳定生产闭环：本次修复与暴露问题清单

- 日期：2026-09-11（Asia/Shanghai）
- 工作区：`D:\workspace\YeQianWorkSpace\yeqian\storyOS`
- 分支 / HEAD：`story-platform-v3` @ `c10c4c7`
- 证据口径：只写当天在本 checkout 能复核的结果；历史报告、memory 与旧结论仅作对照，不当证据。
- 工作树：本轮修复仍是未提交改动（HEAD 未变，仍为 `c10c4c7`）；第 8 节为用户本轮修复补记。

## 0. 一页速览（问题 / 暴露位置 / 时间 / 是否解决）

> 状态口径：✅ 已解决（本轮修复且有验证）｜🟡 部分解决或有兜底｜❌ 未解决。
> 用户本轮修复版本（2026-09-11 14:2x）见第 8 节：`codex_subscription_image.py` 让每个图片 worker 使用独立 `CODEX_HOME`；`runtime-runner-state.json` 已由真实 runner 循环写出。
> 统计：17 条中 3 条已解决、3 条部分解决、11 条未解决。时间均为本机时区（Asia/Shanghai）。

| # | 问题 | 在哪里暴露 | 时间 | 是否解决 |
| --- | --- | --- | --- | --- |
| 修复A | 出图链没接进主入口（`continuous_host_loop` 是死开关） | `config/storyos.yaml:157` + EP003 手工跑 `image_scheduler run` | 2026-09-11 13:13–14:23 | ✅ 已解决（本轮改代码，离线 16 条回归 + 真实数据干跑；14:23 已真实跑通一轮，`runtime-runner-state.json` = HOST_WAIT / rc=20） |
| 修复B | 图已生成，但 CLI 保存冲突被当成静默技术失败 | `episodes/10_彼此的天上/03_雾中的另一座生活区/meta/image-workers/01-9ae48137181c-a1.jsonl` | 2026-09-11 13:45；14:2x 补根因 | ✅ 已解决（分类 + 恢复 + 不占内容返修额度；14:2x 补每 worker 独立 `CODEX_HOME`，绕开并发写冲突） |
| W-01 | 发布后闭环零证据（无 `PUBLISHED` / `DATA_REVIEWED`） | 全部 `episode-state.json` 状态盘点 | 2026-09-11 14:00 | ❌ 未解决 |
| W-02 | 媒体与仓库分离，`PUBLISH_READY` 校验失败 | `validate_episode.py .../09_旧物怪谈/05_婚礼前夜_记忆麻醉 --target PUBLISH_READY` | 2026-09-11 14:05（源头 09-10） | ❌ 未解决 |
| W-03 | 运维载体没注册系统任务 | `scripts/phase9_runtime_deploy.py status --apply` | 2026-09-11 14:03 | 🟡 手工进程在跑（18081 health/metrics 200），重启不自愈 |
| W-04 | Web Console 没接后端，环境变量名还不一致 | `web-console/.env.example` vs `web-console/src/api/client.ts:9` | 本轮代码检查（无更早记录） | ❌ 未解决 |
| W-05 | 图片通道单点，失败会静默 | `reports/09-05婚礼前夜生产问题复盘_20260910.md:19-26` + 本轮 a1 冲突 | 2026-09-09 ~ 09-11 | ✅ 已修复（第 8 节：每 worker 独立 `CODEX_HOME`，不再共享写目录）；真实并发出图未复验 |
| W-06 | 额度熔断把内容返修与技术重试混在一起 | 同上文档第 2 节 + `meta/runtime/raw-candidate-budget-override.json` | 2026-09-09 14:18 | 🟡 隔离机制已在代码确认（claim 只在候选落盘后 commit，技术失败 release）；全篇额度耗尽仍无常规出口 |
| W-07 | 封面/快照不在帧变更的失效传播链上 | 同上文档第 4 节 | 2026-09-10 | ❌ 未解决 |
| W-08 | 冻结期无写锁，且缺官方 void 出口 | 同上文档第 10 节 | 2026-09-10 17:02–17:05 | ❌ 未解决 |
| W-09 | 性能账本字段缺失，不能当工时/额度依据 | 同上文档第 8 节 | 2026-09-10 | ❌ 未解决 |
| W-10 | golden 回归注册表是空的，回归跑不起来 | `reports/golden-episode-registry.json` | 2026-09-11（本轮检查） | ❌ 未解决 |
| W-11 | 报告状态与实际状态漂移 | `reports/phase9_production_readiness_gate.md:165` vs `scripts/phase9_production_switch.py status` | 切换 2026-09-11 10:05，14:03 复核 | ❌ 未解决（改文案即可） |
| W-12 | 本机 skill 因 BOM 被 Codex 拒绝加载 | `meta/image-workers/01-...-a2.jsonl` 的 `codex_core::session` ERROR | 2026-09-11 13:46 | ❌ 未解决（仓库外文件，去 BOM 即可） |
| W-13 | `legacy_notify` hook 报 os error 206 | 同目录 a1 / a2 日志 `hook_runtime` WARN | 2026-09-11 13:45 / 13:48 | ❌ 未解决（噪声） |
| W-14 | 图片 worker 日志混入网络类 WARN | 同目录 a2 日志 | 2026-09-11 13:46 | ❌ 未解决（噪声） |
| W-15 | 派生缓存 `next-action.json` 停在旧结论 | `episodes/10_彼此的天上/03_雾中的另一座生活区/meta/runtime/next-action.json` | 2026-09-11 13:13 写入；14:23 重算 | 🟡 14:23 已重算并写入 `derived_at`；新宿主循环每轮重算，读侧仍无时间戳校验 |

## 1. 结论（先看这里）

**一句话：现在还不能算“稳定生产闭环”。**

- 本次修掉了三个具体断点：①自动出图链没接进主入口；②图片保存冲突被当成静默技术失败；③并发 worker 共享 `CODEX_HOME` 触发保存冲突（改为每 worker 独立目录，第 8 节）。
- 但“生产 → 发布 → 数据复盘”的最后一公里没有任何一集走通：4 集停在 `PUBLISH_READY`，`PUBLISHED` 与 `DATA_REVIEWED` 均为 0 集。
- 平台侧 V3 Runtime 归属已经切换、常驻 Worker 正在跑，但系统服务/计划任务没注册，“重启后能不能自愈”没有验收。
- 所以当前真实状态是：**单篇可以从选题一路走到 `PUBLISH_READY`（需要人盯宿主动作）；发布之后全部未验证。**

分层判定见第 4 节；暴露问题清单见第 3 节。

## 2. 本次修复的两个问题（A/B；本轮用户修复版本见第 8 节）

### 2.1 问题 A：自动出图链没有接进主入口

“停在人工触发图片生成这一步”这个判断**基本属实，但要说准确**：不是没有出图能力，而是没有代码自动调用出图调度器，出图要靠人手工执行 `image_scheduler.py run`。

证据（今天在本 checkout 逐条复核）：

- `config/storyos.yaml:157` 写着 `runtime.continuous_host_loop: true`，但修复前全仓库没有任何代码读取它（`rg continuous_host_loop` 只命中配置文件本身）→ 死开关。
- `episodes/_system/runtime_dag.py`：WORK/WEB 且未显式 `--codex` 时，`scoped_model` / `scoped_codex` 步骤直接返回 `HOST_ACTION_REQUIRED_RC = 20`（HOST_WAIT），把执行权交回宿主。
- `episodes/_system/episode_runner.py` 本来就有出图分发逻辑（`GENERATE_IMAGES` / `RETRY_TECHNICAL_FAILURES` / `REPAIR_FAILED_IMAGES`），但 `story_os.py` 没有 `runner` 入口，只能按脚本路径手工调用；且全仓库不存在 `meta/runtime-runner-state.json`，说明 runner 从未真正跑过。
- 现实现场证据：`episodes/10_彼此的天上/03_雾中的另一座生活区/meta/image-workers/` 里帧 01 的两次 attempt（13:45 / 13:49）都来自手工启动的进程。

修复：

| 文件 | 改动 |
| --- | --- |
| `episodes/_system/episode_runner.py` | 抽出纯决策函数 `local_image_action(action)` 与执行函数 `run_local_image_action()`，`execute_cycle()` 复用它们，行为不变 |
| `episodes/_system/workflow_runner.py` | 新增 `continuous_host_loop_enabled()` / `host_loop_step()` / `advance_host_loop()`；DAG 返回 HOST_WAIT 且开关为真时，先执行本机可执行的图片动作，再恢复 DAG，结果写入 performance 与 checkpoint（步骤名 `HOST_LOOP`） |
| `episodes/_system/story_os.py` | 新增 `story_os.py runner <episode_dir> [--interval N] [--resume]`，转发到 `episode_runner.py`（仍走 `persistent_runner_daemon` 的单所有权锁与心跳） |

修复后的链路：

```
story_os.py run <episode> --full-auto
  → workflow_runner.execute
  → runtime_dag.execute  →  rc=20 HOST_WAIT
  → (continuous_host_loop=true) advance_host_loop
       本地可执行动作 → episode_runner.run_local_image_action → image_scheduler / batch_scheduler
       → 重新 runtime_dag.execute
  → 遇到宿主动作（CREATIVE_STORY / PREIMAGE_COMPILE / VISUAL_LOCK / RELEASE / 评审）立刻交回，rc 原样返回
```

边界（必须知道）：

- 只自动执行“本机图片执行层”的动作；Story、Preimage、Critic、Gate、Release 仍交回宿主，符合 AGENTS 的 runtime 路由规则。
- 循环上限 `HOST_LOOP_MAX_CYCLES = 12`；到顶就返回 HOST_WAIT，不会无限空转，也不会把宿主动作伪造成 PASS。
- 已做一次真实数据干跑：把 `episodes/10_彼此的天上/03_雾中的另一座生活区` 的 `meta`/`docs`/`prompts` 复制到 `.storyos/_verify_hostloop/`（跑完删除，不碰原目录）：`next_action.write()` → `VISUAL_LOCK / WORK`，`host_loop_step()` → `(False, 'VISUAL_LOCK')`，即遇到宿主动作立即交回，没有触发任何出图；该临时目录已清理。
- 仍未在真实新篇上从 `IDEA_LOCKED` 跑到 `PUBLISH_READY` 做整篇端到端验收（见第 6 节第 1 条）。
- 14:23 复核：`episodes/10_彼此的天上/03_雾中的另一座生活区/meta/runtime-runner-state.json` 已存在（status=HOST_WAIT、return_code=20、pid 3320、heartbeat 14:23:00），`meta/runtime/next-action.json` 也已重算为 `VISUAL_LOCK / WORK` → 修复 A 的链路已真实跑过一轮，不再只有离线干跑。

### 2.2 问题 B：图片保存冲突被当成静默技术失败

原始证据（`episodes/10_彼此的天上/03_雾中的另一座生活区/meta/image-workers/01-9ae48137181c-a1.jsonl`）：

```
{"type":"thread.started","thread_id":"01a08efd-3a7f-78d2-a60c-b15adc497218"}
2026-09-11T05:45:10Z  WARN codex_image_generation_extension::tool: failed to save generated image:
  当文件已存在时，无法创建该文件。 (os error 183)
  call_id=exec-863861d5-1e96-49b1-9908-139a45c06cb4
  output_dir=C:\Users\79873\.codex\generated_images\01a08efd-3a7f-78d2-a60c-b15adc497218
```

现象：图其实已经生成，但 Codex CLI 自己保存失败；Story OS 只看到 `rc=0; no_valid_image=true`，于是记为一次“静默技术失败”→ 重出 → 烧额度与墙钟。

根因边界：冲突发生在 `codex.exe` 内部（`D:\soft\nvm\v22.22.0\node_modules\@openai\codex\...\codex.exe` 里含 `failed to save generated image: ` 字样）。**Story OS 无法 patch CLI**，只能在系统侧做三件事：认出它、尽量捡回产物、别把它算成内容返修。

修复：

| 文件 | 改动 |
| --- | --- |
| `episodes/_system/image_model_policy.py` | 新增 `ARTIFACT_SAVE_COLLISION="PROVIDER_ARTIFACT_SAVE_COLLISION"` 与匹配模式（`failed to save generated image` / `os error 183` / `error_already_exists` / `当文件已存在时`），分类顺序放在 RATE_LIMIT 之后、BACKEND_5XX 之前 |
| `episodes/_system/image_artifact_collector.py` | 恢复根目录改为 `CODEX_HOME` 感知（`codex_home()` / `generated_images_root()`）；新增按 `call_id=` 的二级恢复；兼容路径补 `root / name` 探测 |
| `episodes/_system/image_scheduler.py`（无需改） | 该 code 不在 `NON_REGENERATING_FAILURE_CODES` 集合里，因此仍按“技术失败可重试”处理，不消耗每帧一次的内容返修额度 |

验证：

- `python episodes/_system/image_model_policy.py self-test` → `IMAGE MODEL POLICY V2.1 SELF-TEST PASS`（含新分类断言）。
- `python episodes/_system/image_artifact_collector.py` → `IMAGE ARTIFACT COLLECTOR V2.6.2 CODEX_HOME THREAD-SCOPED SELF-TEST PASS`。
- 新增离线回归 `tests/system/test_runner_host_loop_v262.py`（16 条）：冲突分类、不被判成 model unavailable、不占内容返修、call_id 提取、`CODEX_HOME` 覆盖、按 call_id 找回产物、本地出图动作识别、宿主动作交回、循环有界、`execute_cycle` 优先本地动作。
- 真实现场：同帧 attempt 2（13:49）成功产出，队列项 `9ae48137181c` 状态 `generated`。

剩余边界：

- 若 CLI 冲突后没有留下可恢复文件，Story OS 只能重试，无法从像素层救回（本次 a1 就是这种情况）。
- 多个 Codex 图片 worker 共享同一个 `CODEX_HOME` 时，写冲突概率随并发上升（本次是 3 并发）。→ 本轮用户修复版本已在 `episodes/_system/codex_subscription_image.py:226-235` 让每个 worker 用独立 `CODEX_HOME`（详见第 8 节）；真实并发尚未复验。

## 3. 目前暴露、尚未闭环的问题

### A 组：直接阻断“稳定生产闭环”

| ID | 问题 | 今天复核到的证据 | 状态 |
| --- | --- | --- | --- |
| W-01 | 发布之后没有任何闭环证据 | 全部 10 个 `episode-state.json`：`PUBLISH_READY` ×4（`09_旧物怪谈/04_瓶中世界`、`09_旧物怪谈/05_婚礼前夜_记忆麻醉`、`10_山难伪纪录片/01_鳌太线_热汤`、`11_仲夏夜惊魂/01_停电夜蜕壳`），`IDEA_LOCKED` ×4，`VISUAL_CALIBRATED` ×1，`STORYBOARD_LOCKED` ×1；`PUBLISHED` = 0，`DATA_REVIEWED` = 0 | 未闭环 |
| W-02 | 媒体与仓库分离：`PUBLISH_READY` 校验在“唯一事实源”上失败 | `validate_episode.py .../09_旧物怪谈/05_婚礼前夜_记忆麻醉 --target PUBLISH_READY` → 6 条 FAIL：`manifest.release.publish_dir` / `cover_path` 缺失、`concept_ambition_gate candidates_sha256 drift`、`visual_lock_v21_gate calibration image missing`、`resolved_frame_contract_gate CHARACTER_APPEARANCE_ANCHOR_STALE`、`final_candidate_snapshot_gate frame 01 approved_asset.path missing`；同一路径在 `D:\workspace\YeQianWorkSpace\yeqian\aigc-dali-cat\...\media\approved\01.png` 里存在（`Test-Path` = True） | 未修（工作区拆分，非丢失） |
| W-03 | 生产载体“在跑”但不在系统服务/计划任务里 | `python scripts/phase9_runtime_deploy.py status --apply` → `Get-ScheduledTask -TaskName 'StoryOSRuntime'` 未找到；同时 18081 端口**有**监听（pid 42420 python，`/healthz` 200、`/metrics` 200，`storyos_runtime_worker_ticks_total = 193`）→ 常驻 Worker 是手工进程，重启/开机不自愈 | 半成品 |
| W-04 | Web Console 未接后端，且环境变量名不一致 | `web-console/.env.example` 定义 `VITE_API_BASE_URL`，而 `web-console/src/api/client.ts:9` 读的是 `import.meta.env.VITE_PLATFORM_API_URL`；`platform/api/controllers.py` 没有对应的 HTTP 服务接线证据 | 未接通 |

### B 组：高风险工程债（不必然立刻炸，但会持续烧额度/污染证据）

| ID | 问题 | 证据 | 状态 |
| --- | --- | --- | --- |
| W-05 | 图片通道是单点，失败会静默/半静默 | `reports/09-05婚礼前夜生产问题复盘_20260910.md:19-26`：全篇 20 张共 69 次生成，技术失败 25 次落 8 帧，帧 01 独占 16 次；25 次里 12 次（48%）是“worker 退出但无产物”（`rc=0`/`rc=1`，`no_valid_image=true`）。今天的 `os error 183` 属同类 | 已补每 worker 独立 `CODEX_HOME`（第 8 节），真实并发待复验 |
| W-06 | 额度熔断把“内容返修”和“技术重试”混在一起，且没有出路 | 同上文档第 2 节：2026-09-09 episode 额度 35/35 用尽后熔断连坐；解除只能靠用户授权改 `raw_candidate_budget.py` + 写 `episodes/09_旧物怪谈/05_婚礼前夜_记忆麻醉/meta/runtime/raw-candidate-budget-override.json`（该文件今天仍在，`Test-Path` = True） | 配额隔离已在代码确认；额度耗尽仍靠人工 override |
| W-07 | 封面/快照不在帧变更的失效传播链上 | 同上第 4 节：帧 SHA 变更不自动作废封面、contact sheet、snapshot、交付包 | 未修 |
| W-08 | 冻结期没有写锁，也缺官方 void 出口 | 同上第 10 节：外部会话在冻结期重出帧 01，失败后只能 `tech-fail` + 手工 `lock` 补回；作废不归还例外额度 | 未修 |
| W-09 | 性能账本字段缺失，不能当工时/额度依据 | 同上第 8 节：`stage_wall` 空、`image_attempts` 空（实际 69 次）、`production-ledger.attempts = 0`、`batches = []` | 未修 |
| W-10 | golden 回归注册表为空，回归跑不起来 | `reports/golden-episode-registry.json` = `{"episodes": [], "recommended_minimum": 10}` | 未修 |
| W-11 | 报告状态与实际状态漂移 | `reports/phase9_production_readiness_gate.md:165` 仍写“Production Ownership Switch … 待授权”，但 `python scripts/phase9_production_switch.py status` → `primary=V3_RUNTIME previous=V2_RUNTIME reason=production_migration_finalized updated_at=2026-09-11T02:05:21Z`，`meta/runtime/runtime-primary.json` 确认 V3 | 文案过期 |
| W-15 | 派生缓存 `next-action.json` 会停留在旧结论 | `episodes/10_彼此的天上/03_雾中的另一座生活区/meta/runtime/next-action.json` 写于 13:13:52，内容是 `PREIMAGE_COMPILE / preproduction handoff is missing or stale`；但 `meta/preproduction-handoff.json`（13:20:06）之后才落盘，现在重算 `next_action.derive()` 得到 `VISUAL_LOCK / WORK`。人工打开这个文件会看到过期的“当前阻塞” | 有界缓解：新宿主循环每轮先 `next_action.write()` 重算，读到的始终是新鲜值；旧文件本身仍未加时间戳校验 |

### C 组：噪声与低成本修复项

| ID | 问题 | 证据 | 建议 |
| --- | --- | --- | --- |
| W-12 | 本机 skill 因 BOM 被 Codex 判为无 frontmatter | `meta/image-workers/01-9ae48137181c-a2.jsonl`：`ERROR codex_core::session::session: failed to load skill C:\Users\79873\.codex\skills\douyin-ai-horror\SKILL.md: missing YAML frontmatter delimited by ---`；实测该文件前 3 字节为 `EF BB BF`（UTF-8 BOM），而首行确实是 `---` | 去掉 BOM 即可（仓库外文件，本次未改） |
| W-13 | `legacy_notify` after_agent hook 每次都失败 | a1/a2 日志：`hook_name=legacy_notify error=文件名或扩展名太长。 (os error 206)` | 缩短会话/日志路径或关闭该 hook |
| W-14 | 图片 worker 日志混入大量网络类 WARN | 同一日志里 `codex_core_plugins` 插件目录请求失败、`rmcp` MCP `chatgpt.com` 连接失败、`codex_analytics` 上报失败 | 只影响可读性，不阻塞出图 |

## 4. 稳定闭环判定（分层）

| 层 | 判定 | 依据 |
| --- | --- | --- |
| 代码层 | 🟢 本轮全绿 | `python -m pytest tests/platform tests/system -q -p no:cacheprovider --continue-on-collection-errors` → **552 passed / 0 failed / 16 subtests passed**（含本批新增 16 条）。同日上午一次快照出现过 4 failed + 3 collection errors（集中在 production learning loop 模块）；`0a47ca2`、`c10c4c7` 两个提交落地后未再复现 |
| 真实图片通道 | 🟡 可用但脆弱 | 真机出图成功过（含今天 attempt 2）；25/69 次技术失败、约一半是“无产物静默失败”；并发写冲突本轮已加每 worker 独立 `CODEX_HOME`（未复验），CLI 进程内保存冲突仍不可 patch |
| 单篇端到端 | 🟡 到 `PUBLISH_READY` 为止 | 4 集 `PUBLISH_READY`；出图链本轮接上自动触发，14:23 已在 EP003 真实跑通一轮 runner（HOST_WAIT，交回宿主动作），尚未跑到整篇交付 |
| 发布闭环 | 🔴 无证据 | `PUBLISHED` = 0、`DATA_REVIEWED` = 0；`post_publish_review` 与 6h/24h/48h/7d 回填从未真实走完 |
| 运维载体 | 🟡 进程活着，没绑系统 | 18081 health/metrics 200、worker ticks=193，但计划任务未注册；“重启即恢复”未验收 |

结论：**“稳定”目前只成立于代码层与单篇到 `PUBLISH_READY`；作为可交付的生产闭环，还缺发布验证与运维固化。**

## 5. 复现与验收命令

```powershell
# 配置
python episodes/_system/story_os.py config validate          # → STORY OS CONFIG VALID
# 本轮修复的自测
python episodes/_system/image_model_policy.py self-test      # → IMAGE MODEL POLICY V2.1 SELF-TEST PASS
python episodes/_system/image_artifact_collector.py          # → IMAGE ARTIFACT COLLECTOR V2.6.2 … PASS
python episodes/_system/workflow_runner.py self-test         # → WORKFLOW RUNNER V2.1 SELF-TEST PASS | PHASE910
python episodes/_system/story_os.py runner --help            # → 新增 runner 入口
# 回归
python -m pytest tests/system/test_runner_host_loop_v262.py -q -p no:cacheprovider   # → 16 passed
python -m pytest tests/platform tests/system -q -p no:cacheprovider --continue-on-collection-errors
# 平台/运维现状
python scripts/phase9_production_switch.py status            # → primary=V3_RUNTIME
python scripts/phase9_runtime_deploy.py status --apply       # → StoryOSRuntime 任务未注册
python episodes/_system/validate_episode.py "episodes/09_旧物怪谈/05_婚礼前夜_记忆麻醉" --target PUBLISH_READY
```

## 6. 建议处理顺序

1. **端到端验收自动出图链**：拿一个 `STORYBOARD_LOCKED` 的新篇（如 `episodes/10_彼此的天上/03_雾中的另一座生活区`），用 `story_os.py run <ep> --full-auto` 跑到需要宿主动作的位置，确认出图不再需要手工 `image_scheduler.py run`，并留下 `HOST_LOOP` performance/checkpoint 证据。
2. **打通发布闭环**：选一集 `PUBLISH_READY` 真实发布，用 `post_publish_review.py mark-published` 推进 `PUBLISHED`，再按 6h/24h/48h/7d 回填，让 `DATA_REVIEWED` 第一次成立。
3. **收敛 W-02 工作区拆分**：明确媒体目录的权威位置，在权威 checkout 上重跑 `PUBLISH_READY` 校验，避免“文件在，但事实源看不到”。
4. **固化运维载体（W-03）**：得到授权后注册计划任务或系统服务，验收 principal/trigger、18081 监听、`/healthz` 200 与重启自愈。
5. **图片通道稳态（W-05/W-06）**：每 worker 独立 `CODEX_HOME` 已落地（第 8 节），待真实并发出图复验；熔断把内容返修与技术重试彻底分开；把按帧授权额度变成常规能力而非一次性 override。
6. **证据链补强（W-07/W-08/W-09/W-10）**：封面/快照纳入失效传播链、提供官方 void 出口、修性能账本字段、填充 golden 注册表。
7. **低成本噪声（W-12/W-13/W-14）**：去 BOM、修 hook、收敛网络类 WARN。

## 7. 本次改动文件

| 文件 | 说明 |
| --- | --- |
| `episodes/_system/image_model_policy.py` | 新增 `PROVIDER_ARTIFACT_SAVE_COLLISION` 分类与自测 |
| `episodes/_system/image_artifact_collector.py` | `CODEX_HOME` 感知 + call_id 二级恢复 + 自测 |
| `episodes/_system/episode_runner.py` | 抽出 `local_image_action` / `run_local_image_action` |
| `episodes/_system/workflow_runner.py` | 接通 `runtime.continuous_host_loop`，新增 `HOST_LOOP` 证据 |
| `episodes/_system/story_os.py` | 新增 `runner` 子命令 |
| `episodes/_system/codex_subscription_image.py` | 每个图片 worker 独立 `CODEX_HOME`（用户本轮修复，详见第 8 节） |
| `tests/system/test_runner_host_loop_v262.py` | 新增 16 条离线回归 |
| `reports/Story_OS_稳定生产闭环_暴露问题清单_20260911.md` | 本文 |

## 8. EP003 生产稳定性修复（W-05/W-06）2026-09-11

### W-05 图片通道单点失败修复

问题：

多个 Codex 图片 worker 并发执行时，共享默认 `CODEX_HOME`，可能同时写入同一个生成产物目录，触发：

```
PROVIDER_ARTIFACT_SAVE_COLLISION
os error 183
failed to save generated image
```

修复：

`episodes/_system/codex_subscription_image.py`

- 每次图片 worker 创建独立临时 `CODEX_HOME`。
- worker 生命周期内隔离 Codex 临时产物目录。
- 不改变 Story OS raw/output/release 权威资产路径。
- 保留原有 artifact collector 恢复机制。

修复后：

```
worker-1
  CODEX_HOME=/tmp/.../worker-1

worker-2
  CODEX_HOME=/tmp/.../worker-2

worker-3
  CODEX_HOME=/tmp/.../worker-3
```

避免并发写冲突。

### W-06 技术失败与内容返修额度隔离确认

当前生产策略调整为：

```
内容质量问题
    ↓
content repair budget
    ↓
消耗返修额度


技术异常
(timeout / save collision / worker crash)
    ↓
technical retry
    ↓
不消耗内容返修额度
```

已有机制确认：

- `PROVIDER_ARTIFACT_SAVE_COLLISION` 已进入技术失败分类。
- image worker 失败路径会 release 未提交 candidate claim。
- 未产生有效候选图时，不提交内容候选额度。
- 技术重试不会伪装成内容返修。

EP003 验收目标：

- 图片生成失败可重试。
- 技术异常不会耗尽内容返修额度。
- 多 worker 并发不会共享 Codex 临时写目录。

当前状态：

```
W-05: ✅ 已修复
W-06: ✅ 已完成隔离确认
```

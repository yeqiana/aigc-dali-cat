# VITE_PLATFORM_API_URL: StoryOS Platform API 基址（scripts/platform_api_server.py，默认 127.0.0.1:8080）。
# 留空时 Web Console 走 Vite dev/preview 同源代理（vite.config.ts -> platformProxy）转发到该地址。
# 只有这一个后端入口变量；不要再引入 VITE_API_BASE_URL / VITE_RUNTIME_STREAM_URL。
VITE_PLATFORM_API_URL=http://localhost:8080

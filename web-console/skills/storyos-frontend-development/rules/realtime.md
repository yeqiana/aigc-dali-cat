# Rule: Realtime & React Performance

## 实时监控规范
- **禁止全局刷新**：严禁使用 `setInterval(() => location.reload(), 5000)` 这类粗暴刷新；
- **状态不丢失原则**：自动轮询与数据刷新时，绝不得重置用户的滚动条位置、已展开的 Drawer、已选中的 Tab、Filter 条件及搜索框内容；
- **高频状态局部下沉**：5 秒心跳、倒计时与帧生成进度必须作为局部组件状态独立维护，避免触发整个页面根树全量重渲染。

# Workflow: Review

## 适用场景
代码发布前代码走查、交互审计、视觉一致性审计。

## 执行步骤
1. **Instruction Priority 检查**：确认无外部规则覆盖 StoryOS 专有约束。
2. **Operate 模式扫描性检查**：一屏是否能快速识别四个核心问题。
3. **状态契约合法性**：是否存在纯颜色标识或非法动作按钮（如 PASSED 出现跳过）。
4. **性能与内存检查**：是否存在全量重渲染、轮询重置上下文风险。
